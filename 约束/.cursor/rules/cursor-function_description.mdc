---
description: 通用功能解读——链接目录；全场景枚举；FC全量取值维度；广告须写清预加载/展示/阻塞/重试/补货；开屏须验收UMP预加载→loading结束查缓存(禁止现场load)与跨页缓存存活；有AB面须查中途A→B对广告影响；每位须有load失败重试与补货(开屏展示回调即补)；接入广告后须输出请求全链路确认清单供用户确认；对话完整+镜像备份md（固定写入 skill/约束/.cursor/{桌面app名}/）
alwaysApply: true
---

当用户要求**解读 / 说明 / 分析 / 讲清楚**某一功能、模块、类、方法、流程（如「AB 面怎么判」「归因怎么做」「放行闸逻辑」）时，严格按本规范输出。

**目标**：把代码变成可读文档，**产品、开发、测试都能看懂**——不仅知道「调了哪个函数」，更知道「这一步在业务上干什么」。用户无需逐行读源码即可理解：做什么、怎么做、有哪些分支、弱网/异常会怎样。

**完备性铁律（每次解读必须遵守）**：

1. **先穷尽再落笔**：解读前必须在代码中**清点**本功能链路上所有超时点、远程拉取点、续作点、空态与异常兜底；**禁止**只写主路径成功分支。
2. **超时不可省略**：凡代码存在 `withTimeoutOrNull` / `deadline` / `*TIMEOUT_MS` / `awaitFc*` / `fetchActivate`，§2.2 时序、§2.4 流程图、§2.5 场景矩阵**必须**各有对应行/边/场景；**禁止**流程图只写「拉 Firebase / 拉 FC」而不画**超时后用什么继续**（缓存 / 本地默认 / 占位结论）。
3. **远程拉取三层都要写**：`fetch 进行中` → `fetch 成功` → `fetch 超时/失败用缓存`；若另有 `awaitFc*` 等待层，须再写 **等待 FC deadline 超时** 场景（与 fetch 超时是两层，不可合并成一句）。
4. **并行竞态须写时刻**：多任务并行时，**禁止**把某一时刻的常态写成全程行为；须写「**在某某调用瞬间** / **若某某已完成**」的分支（见 §1.6.5、§1.7.6）。
5. **自检不通过则补写**：§2.10 任一项未勾选，**不得**交付解读；缺 Firebase 超时、缺续作、缺空 refer、缺竞态分支等任一场景视为不合格。
6. **与它有关就要详细说明**（§1.10）：功能链路上每一个**阶段 / 子轨 / 子状态 / 续作链路**，只要在代码或场景矩阵里被提到，就必须在 **§2.11 分阶段详细说明** 中有**独立专节**写清判定、时序、标志位、超时、分支与衔接；**禁止**只在 §2.5 写一行、§2.9 写一句、或 §2.7 只挑「重点」而省略阶段2 等子阶段。
7. **广告功能必须写透预加载与展示**（§1.11）：解读广告变现、单广告位、AdLoader、预加载编排时，**每个** `AdSense` 位须在 **§2.12 广告位专表** 中写清**预加载时机**、**展示时机**、**展示成功处理**、**展示失败处理**（含无缓存、load 超时、SDK 错误、闸门未过、日配额满等分支），**禁止**只写「有广告」或只列广告位名称。
8. **广告预加载须分析阻塞**（§1.11.8）：接入/解读广告相关功能或涉及广告 Skill 时，**每一次**预加载须判定是否**阻塞式**（suspend/await/串行 await 下一 load）；单 load、同协程多类型连续 preload、多协程并行 preload 均须说明**阻塞谁、阻塞多久、对展示与收益的影响**；**凡出现阻塞必须显式写出**，禁止默认「后台不阻塞」。
9. **接入广告后须输出请求全链路确认清单**（§1.11.9 / §2.13）：**穷尽**所有广告**请求时机**（含预加载、现场 load、补货、Banner 重建等）与**预加载点**；标注**是否阻塞式请求**；逐条评估**请求时机位置的收益隐患**；**列出清单供用户确认**后，方可声称「广告已接入完成」。
10. **有 AB 面时须查广告与面别切换**（§1.11.10）：项目已接入 AB 面时，解读/接入广告**必须**写清 preload/show 时刻面别、**中途 A→B** 后已缓存广告是否仍可展示、升 B 后 B 专属位是否补 preload/show；无 AB 则标「不涉及」。
11. **开屏须验收标准链路**（§1.11.11）：UMP 后 preload → loading 动画结束**仅查缓存** → 有则 show、无则 skip（**禁止** loading 结束现场 load）；须核对 skip/页面销毁后 preload 仍继续、Loader **仍保留**开屏缓存（含「进主页后才 load 完仍入缓存」）。
12. **每位须有 load 失败重试**（§1.11.12）：**每一个** AdSense 位须写清失败重试（次数、间隔、锚点）；源码缺失须标「**缺失/待接入**」，不得默认通过。
13. **每位须有补货机制**（§1.11.13）：**每一个** AdSense 位须写清 consume/show 后补货；**开屏**：**展示回调触发**（如 `onImpression` / show 成功）即 preload 下一条，**用完即加载**。

**读者**：默认同时服务**产品经理**（业务语义、用户感知）与**开发**（代码锚点、分支条件）。**禁止**在流程图、时序表、场景说明里只写 `AppAdsBootstrap.run`、`startSettlement` 等名词而不解释其含义。

**全局双语与准确表述**：所有对话涉代码时另遵守 `@code-term-bilingual`（每个代码名词附中文业务解读，禁止「必定/总是」类误导简写）。

**适用范围**：任意业务功能（不限于启动链路）。若同时涉及「Application → 启动页结束」全链路，可叠加 `@cursor-ai-launcher`；本规则聚焦**用户点名的单个或一组功能**。

---

## 0. 规则标识（每次回复必显）

本规则已启用（alwaysApply: true）。**每次回复的第一行**必须输出：

> 📌 已启用约束：`功能解读约束`

要求：

- 放在回复最开头，正文之前
- 纯问答、方案讨论、写代码交付时都要显示
- **涉广告预加载阻塞分析**时，可在同条或次行补充：`📌 已启用约束：广告预加载阻塞分析（§1.11.8）`
- **接入/解读广告且输出请求全链路清单**时，可在同条或次行补充：`📌 已启用约束：广告请求全链路确认清单（§1.11.9）`
- **涉 AB 面 + 广告**时，可补充：`📌 已启用约束：AB面中途切换与广告展示（§1.11.10）`
- **涉开屏标准链路验收**时，可补充：`📌 已启用约束：开屏UMP预加载与loading查缓存（§1.11.11）`
- **涉广告重试/补货验收**时，可补充：`📌 已启用约束：广告load重试与补货（§1.11.12/§1.11.13）`
- 不要省略或合并到其他段落里

## 1. 解读范围

（必须先写，2～5 句。）

| 项 | 内容 |
|----|------|
| 功能名称 | 用户点名的功能（业务语言） |
| 代码锚点 | 文件路径 + 类/方法（可多个） |
| 边界 | 本次解读包含 / 不包含什么 |
| 关联子功能 | 依赖项列表（将递归展开）；**另须列出本功能涉及的全部阶段/子轨**（见 §1.10 阶段清点表） |

---

## 1.5 分析前置：易忽略链路扫描（必须先做，再写正文）

解读前**必须先读代码**并主动搜索以下模式；命中任一项则**不得一笔带过**，须在 **§2.11 分阶段详细说明** 写透该阶段，并在 §2.9 写续作对照（若会修订结论）：

| 扫描关键词/模式 | 典型含义 | 示例 |
|----------------|----------|------|
| `阶段1` / `阶段2` / `phase1` / `phase2` / `extended` / `Extended` | 多阶段异步，后续阶段可能改结论 | 归因 6s → 30min 阶段2 |
| `needs*Retry` / `*Pending` / `*Done` / `*InFlight` | 超时后标记，后台续作 | `needsExtendedReferrerRetry` |
| `onComplete` / `onReady` / `dispatch` / `callback` 之后的 `launch` | 首次回调完成后还有第二段逻辑 | commit 后 `startExtended*` |
| `apply*Update` / `*IfChanged` / `*AfterExtended` / `*Overrides*` | 已提交结论被事后修订 | `applyModeUpdateAfterExtended` |
| `fastPath` / `hotResume` / `skip` + 并行另一轨 | 快路径与完整路径并存 | 热启动跳过 settlement |
| `commit` / `FINAL` / `resolved` 之后的 IO 协程 | 先锁定状态，后台再算 | 先 commit A，后可能升 B |
| `addListener` / `notify*` / `on*Completed` | 监听触发二次刷新 | FC 完成后补升/降级 |
| `withTimeoutOrNull` / `deadline` / `retry` / `maxAttempts` / `*TIMEOUT_MS` | 超时后有 fallback **和** 可能的重试 | referrer 6s；FC fetch 8s |
| `fcReady` / `awaitFc*` / `fetchOk` / `fetchAndActivate` / `onFirebaseFetchCompleted` | Firebase **拉取超时**与**判面等待 FC 超时**是两层 | fetch 8s 用缓存；等 FC 30s |
| `exists(` / `getValue` / `VALUE_SOURCE_STATIC` / `fetchActivate` / `RemoteConfig` | 远程 key **是否存在** vs **取值** 分支不同 | Mode2 总开关 vs 子项 |
| `isCodeSubCheckEnabled` / `applyByMode` / `applyDefaultLocal` | key 不存在时的**默认语义**须单独写场景 | 子项未配=开启 |
| MMKV/`@Volatile` 标志跨阶段 | 跨启动/跨阶段状态 | `EXTENDED_PENDING` |
| `preloadAd` / `preloadAdAwait` / `preloadAfterLoading` | 预加载触发点 | `AdPreloadCoordinator`、各 Activity `onResume` 前 |
| `suspend` / `await` / `withTimeout` + `load` / `preload` | **预加载是否阻塞**调用链 | 须区分 `launch` 火忘 vs `suspend` 等待 vs 串行 `await` |
| `loadAd` / `loadWithInner` | 现场 load；**开屏 loading 结束禁止**（§1.11.11） | `SplashLaunchPipeline` |
| `showAd` / `takeCachedAd` / `bindNativeAd` | 展示路径；开屏/插屏/原生 loading 结束或 show 前**只查缓存** | `AdNavigationCoordinator` |
| `onAdFailedToLoad` / `retry` / `maxRetry` / `backoff` / `reload` | **load 失败重试**（每位必填 §1.11.12） | 各 `*AdLoader` / Fetcher |
| `destroy` / `onDestroy` / `finish` + Loader/`AdCache` | Activity 销毁后广告实例是否仍留缓存 | 开屏页 destroy 不得 discard |
| `loading.*end` / `onLoadingComplete` / `Lottie.*complete` | 开屏 **loading 动画结束** → 查缓存 show/skip | `StartActivity` |
| `commitAbFace` / `applyModeUpdateAfterExtended` / `currentIsModeB` + 广告 | **中途 A→B** 与 preload/show/闸门 | §1.11.10 |
| `showSkippedNoCache` / `resolveDisabledReason` | 无缓存或闸门未过的跳过 | 须写用户是否阻塞 |
| `AdReplenishCoordinator` / `replenishOn*` / `展示后补货` | **每位** consume/show 后补货（§1.11.13）；开屏=展示回调即 load | 成功展示后的下一跳 |
| `onImpression` / `onDismiss` / `ad_no_show` / `ad_request_fail` | 展示成功/失败埋点与回调 | 须对照成功/失败分支 |
| `trackRequest` / `ad_request` / `PromoMetricsSink` / `loadAds` / `AdRequest` | **每一次 SDK 广告请求**（含 preload 与现场 load） | 须纳入 §1.11.9 请求全链路清单 |
| `canShowAd` / `enableFor` / `allowsAdSense` | 展示前闸门 | 每层失败时的行为 |
| `MainBannerController` / `showBanner` | Banner 不走 Loader 缓存的特殊路径 | 每次 onResume 重建 |

**禁止**：只写主路径成功分支；**禁止**用「可能」「后续会更新」一句带过而不写触发条件、超时、修订函数、用户效果。

---

## 1.10 关联阶段详细展开铁律（**与它有关就要详细说明**）

解读某一功能时，凡在代码、时序、场景矩阵、流程图中**与该功能结论或行为有关**的**阶段 / 子轨 / 子状态机 / 续作链路**，均须在 **§2.11 分阶段详细说明** 中**逐段写透**——不得只在 §2.2 时序表占一行、§2.5 矩阵占一行、或 §2.7 写「详见续作」。

### 1.10.1 什么叫「与它有关」

满足**任一**即视为有关，**必须**在 §2.11 有专节：

| 判定 | 示例（AB 面） |
|------|--------------|
| 会改变本功能结论或对外状态 | 阶段2 拉到 refer → A→B |
| 与本功能并行或串行衔接 | FC 轨、AB 轨、老用户快路径 |
| 有独立超时 / 重试 / 状态机 | 阶段1 6s、阶段2 阶梯、等 FC 30s |
| 有 MMKV / 标志位跨阶段持久化 | `EXTENDED_PENDING`、`KEY_ATTRIBUTION_RETRY_INDEX` |
| 在 §1.5 扫描中命中 | `阶段2`、`extended`、`needs*Retry` |
| 在 §2.5 场景矩阵中出现 | 任何带「阶段2」「FC轨」「续作」的 Sxx |

**无关**才可省略：解读范围 §1 已明确「不包含」且代码无调用关系的模块。

### 1.10.2 阶段清点表（写正文前**强制**，附在 §1 解读范围之后或 §2.2 之前）

从源码清点本功能全部阶段/子轨，**一行一个**，不得合并成「后台异步」：

```markdown
### 阶段清点（解读前）

| 序号 | 阶段/子轨名称 | 代码锚点 | 是否阻塞用户 | 是否可能修订结论 | §2.11 专节标题 |
|------|--------------|----------|-------------|-----------------|---------------|
| P0 | … | … | 是/否 | 是/否 | #### P0：… |
```

**AB 面解读**清点表**至少**核对（有则必填，无则标不涉及）：

| 阶段/子轨 | 典型锚点 |
|-----------|----------|
| 冷启入口 / 热启动快路径 | `AppAdsBootstrap.run` |
| FC 轨 fetch | `runFcFetchAndApply` |
| AB 轨 · 等 FC | `awaitFcReadyForMode2` |
| AB 轨 · Mode2 判定 | `resolveNaturalModeBAfterFc` / `evaluateMode2` |
| AB 轨 · commit | `commitAbFace` |
| 归因 · **阶段1** | `AttributionManager.startIfNeeded` → `fetchReferrerWithDeadline` |
| 归因 · **阶段2** | `runExtendedReferrerPhaseIfNeeded` → `runScheduledRetryLoop` |
| 阶段2 · Mode2 修订 | `runExtendedMode2UpdateIfNeeded` |
| FC 完成后补判 | `onFirebaseFetchCompleted` + `naturalModeBIfMasterSwitchOverridesCommitted` |
| 老用户快路径 | `isMode2Resolved` 分支 |

清点表中有几行，§2.11 中**至少**应有几个对应专节（可合并叙述的**仅限**结果完全同路径且差异仅日志的相邻步骤，合并须在表中注明，且合并项 ≤ 20%）。

### 1.10.3 每个阶段专节必写内容（§2.11 统一模板）

每个阶段/子轨专节**禁止**少于下列 10 项（无则写「无」并说明依据）：

1. **阶段身份**：业务上这一阶段干什么；与父功能关系
2. **启动时机**：在哪个函数、**之前/之后**、首次 commit 前还是后
3. **启动条件**：精确布尔表达式（含 MMKV key 名、状态枚举）
4. **不启动条件**：哪些情况跳过本阶段
5. **执行步骤**：逐步业务链 + 代码锚点链（逐步，非一句概括）
6. **超时与重试**：本阶段内每个 deadline、次数、单次超时、失败后下一跳
7. **成功时**：更新哪些字段/标志位、调用谁、对外效果
8. **失败/超时/空数据时**：结论变不变、标志位如何收尾、是否进入下一阶段
9. **与上下游衔接**：上一阶段产出什么、本阶段消费什么、下一阶段谁触发
10. **用户是否感知**：阻塞 / 无感 / 事后才感知（须说明「事后」多久、什么条件下）

**多阶梯/多尝试**（如阶段2）：须**按阶梯逐步**写表（时间点、单次行为、成功/失败分支），**禁止**只写「30s/2m/5m/14m 重试」而不写每一步判断什么、超时多少、成功后调谁。

### 1.10.4 阶段2 类续作 — 最低详细度（命中则 **§2.11 必须有独立专节**）

当功能含「阶段1 占位 + 阶段2 后台续作」时，阶段2 专节**除 §1.10.3 十项外**还须包含：

| 必写块 | 内容 |
|--------|------|
| **进入阶段2 判定** | 列出**每一个**触发条件（超时/异常/空 refer/…）及对应代码函数 |
| **持久化标志位** | pending/done/retryIndex/chainStart 等 MMKV key 表：何时置位、何时清除 |
| **阶梯时间表** | 每个重试点的**相对时间**、**绝对计算方式**（相对 chainStart）、等待逻辑 |
| **单次尝试** | 单次 API 超时 ms、成功/失败/空串/null 各走哪条分支 |
| **单次成功后续** | 更新 refer、是否立即 `onReferrerResolved`、是否重算 Mode2、是否终局打点 |
| **单次失败后续** | retryIndex++、telemetry、是否继续下一阶梯 |
| **终局条件** | 15min / 次数用尽 / 拉到 refer 等；终局时标志位与埋点 |
| **跨启动恢复** | 冷启再次进入时如何从 MMKV 恢复 retryIndex、是否 `finalizeExtendedIfPastDeadline` |
| **修订限制** | 仅 A→B / 不可降 / deadline 后 `isAbRevisionAllowed` |
| **专用子流程图** | 从「阶段2 启动」到「结束」至少一张 mermaid |

**AB 面归因阶段2** 应对照：`shouldStartExtendedRetry`、`needsExtendedReferrerRetry`、`RETRY_SCHEDULE_MS`、`runScheduledRetryLoop`、`applyReferrerUpdate`、`runExtendedMode2UpdateIfNeeded`、`applyModeUpdateAfterExtended`。

### 1.10.5 禁止的偷懒写法

- ❌ 「阶段2 会阶梯重试 refer，15min 内可升 B」（无判定、无逐步、无标志位）
- ❌ 只在 §2.9 写续作、§2.11 完全没有阶段2 专节
- ❌ §2.7 只展开 S01/S02，阶段2 相关 Sxx 写「同续作」
- ❌ 把阶段1+阶段2 合并成「归因」一小节而不分阶段
- ✅ §2.11 有 `#### 归因·阶段2（AttributionManager.runScheduledRetryLoop）` 独立标题，含十项模板 + 阶梯表

---

## 1.11 广告功能解读专则（**涉广告 / AdSense / MonetizationKit 时强制**）

解读「广告功能」「某广告位」「开屏/插屏/原生/Banner」「预加载」「AdLoader」等时，除通用章节外，**必须**满足本节。目标：测试与产品能按文档复现「何时预加载、何时展示、成功/失败后用户看到什么、后台做什么」。

### 1.11.1 适用范围

满足**任一**即触发本节：

- 用户点名广告、变现、AdMob、开屏、插屏、原生、Banner
- 解读范围含 `AdSense`、`MonetizationKit`、`AdLoader`、`AdPreloadCoordinator`、`AdNavigationCoordinator`
- §1.5 扫描命中 `preloadAd` / `showAd` / `takeCachedAd` / `AdReplenishCoordinator`

### 1.11.2 必须先区分的四类策略（写正文前强制）

从源码确认每位属于哪类，**禁止**四类混写成「load 后 show」：

| 类型 | 典型 adType | 预加载 | 展示时是否现场 load | 典型锚点 |
|------|-------------|--------|---------------------|----------|
| **开屏** | SPLASH | **UMP 后 preload**（与 loading 并行） | **否**，loading 结束**仅** `takeCachedAd`；无缓存 skip（§1.11.11） | `SplashLaunchPipeline` |
| **插屏** | INTERSTITIAL | **preload 后台** | **否**，仅 `takeCachedAd` → `showAd` | `AdNavigationCoordinator` |
| **原生** | NATIVE | **preload 后台** | **否**，`bindNativeAd` 绑缓存 | `LanguageActivity`、列表穿插 |
| **Banner** | BANNER | 一般不 preload | **是**，`onResume` 现场 `showBanner` | `MainBannerController` |

> **开屏标准链路（§1.11.11）**：UMP 完成 → preload 开屏 → loading 动画并行 → **loading 结束查缓存** → 有则 show / 无则 skip（**一律禁止** loading 结束现场 `loadAd`）。preload 不因开屏页 destroy 而 discard；进主页后 load 成功仍须入 Loader 缓存。

### 1.11.3 每个 AdSense 位必写四维（§2.12 表列）

对**每一个**工程内存在的 `AdSense` 枚举值（或用户点名的单个位），**禁止合并省略**，须写清：

#### （1）预加载时机

| 必写项 | 要求 |
|--------|------|
| **触发页面/管线** | 如 Splash UMP 后、`LanguageActivity.initView`、`MainActivity` 进主页 |
| **触发函数** | `preloadAd` / `preloadAdAwait` / `preloadAfterLoading` / `AdReplenishCoordinator` |
| **触发条件** | `canShowAd(sense)` 各层闸门；AB 面 B 专属位；语言未配置等布尔条件 |
| **不预加载时** | 闸门 false 时是否静默跳过、是否打日志 |
| **与并行任务关系** | 是否与 UMP / AB commit 并行；**不 await** 谁 |
| **是否阻塞式 load** | **必填**（§1.11.8）：调用方是否 `suspend`/`await` 至 load 结束；阻塞**谁**（跳转/UI/下一广告位/启动放行闸） |
| **同协程多 preload** | 若同一 `coroutineScope` 内连续 preload 多种类型：是否**串行阻塞**（A 完再 B）；是否共享超时 |
| **并行多 preload** | 若 `launch` 多个 preload：是否真并行；SDK/主线程队列是否隐性串行 |

#### （2）展示时机

| 必写项 | 要求 |
|--------|------|
| **用户动作或生命周期** | 如 `onResume`、点击 Continue、底栏切换、进入二级页、返回 |
| **展示前判定** | `canShowAd`、概率（`enterInterShowProbability`）、`takeCached` 是否有货、前台 `isForeground` |
| **是否阻塞用户** | 插屏挡住跳转？无缓存是否直跳？开屏是否占满启动页？ |
| **不展示时** | 走哪条旁路（直跳、隐藏容器、空列表不插广告） |

#### （3）展示成功处理

| 必写项 | 要求 |
|--------|------|
| **成功判定** | SDK 回调：`onImpression`、全屏 `show` 完成、原生 `bind` 成功 |
| **用户后续** | 关广告后跳哪页、`finish`、Tab 切换继续 |
| **后台后续** | `AdReplenishCoordinator` 补货 scene 文案、`recordImpression`、缓存消耗 |
| **埋点** | `ad_show`、`ad_click`、`paid_ad_impression` 等何时上报 |
| **标志位** | 如 `enterForcedShownThisProcess`、日曝光计数 |

#### （4）展示失败处理

须**逐条**覆盖源码存在的失败路径，**至少**核对：

| 失败类型 | 须写清 |
|----------|--------|
| **闸门未过** | UMP 未完成、`enableFor=false`、A 面 B 专属、`allowsAdSense=false`、Premium |
| **无缓存** | 插屏/原生 `takeCached==null` → 直跳/隐藏/400ms 重试 |
| **load 超时** | 开屏 `SPLASH_LOAD_TIMEOUT_MS` → `splashAd=null` → 跳页 |
| **SDK 展示失败** | `ad_no_show`、全屏 show 错误回调 → 是否仍导航 |
| **日配额满** | `canShow`/`canRequest=false` → 跳过展示 |
| **后台/非前台** | `AppForegroundMonitor` 拦截 |
| **弱网/无填充** | NO_FILL、load fail 退避 → 用户是否感知 |
| **load 失败重试** | 次数、间隔/backoff、耗尽后行为（§1.11.12）；每位必填 |
| **补货 load 失败** | 补货 preload 失败是否走重试链（§1.11.13） |

**禁止**只写「失败则不展示」；须写**失败后下一跳函数**（如 `onNavigateNext()`、`startActivity`、`preloadAd` 补货、**重试 schedule**）。

### 1.11.4 广告位清点表（写 §2.12 前**强制**）

```markdown
### 广告位清点（解读前）

| AdSense | adType | A/B 归属 | 预加载锚点 | 展示锚点 | load重试锚点 | 补货锚点 | Loader/特殊路径 |
|---------|--------|----------|------------|----------|-------------|----------|-----------------|
| （每个枚举一行） | | | | | | | |
```

清点表有几行，§2.12 **至少**几行专表；用户只问单位时，该位仍须写满四维，其它位可标「本次不涉及」。

### 1.11.5 广告解读最低场景行数（§2.5 追加）

除 §1.6 通用场景外，**每个 AdSense 位**在 §2.5 **至少**覆盖：

| 场景 | 说明 |
|------|------|
| 预加载成功 | 缓存就绪，等待展示 |
| 预加载跳过（闸门） | 各层闸门失败之一 |
| **预加载阻塞·成功** | suspend/await 路径在超时内 load 成功；写清阻塞时长与后续跳转 |
| **预加载阻塞·超时** | 关键路径 `withTimeout` 到期；写清下一跳与是否仍 preload 其它位 |
| **预加载非阻塞·show 无缓存** | launch 火忘但 show 时 takeCached 为空 |
| **同协程串行 preload·第二位未开始** | 第一位 load 卡住或超时导致第二位未执行（有串行时必填） |
| 展示成功 + 补货 | 展示回调/`onImpression`/`onDismiss` 后 `AdReplenishCoordinator` 或等价 preload（§1.11.13） |
| **load 失败·重试成功** | 首次 fail → retry → 入缓存（§1.11.12） |
| **load 失败·重试耗尽** | 达上限后不再请求或退避 |
| 展示失败·无缓存 | 插屏/原生直跳；**开屏 skip 不现场 load**（§1.11.11） |
| **开屏·loading 结束有缓存** | takeCached 非空 → show |
| **开屏·loading 结束无缓存** | skip 进下一跳；preload 仍继续 |
| **开屏·skip 后 preload 成功** | 进主页后 Loader 仍有开屏缓存 |
| **开屏·页 destroy 后缓存仍在** | Activity finish 不 discard Loader |
| **AB·preload@A 中途升 B** | 缓存/show 闸门变化（§1.11.10，有 AB 时必填） |
| 展示失败·load 超时 | 开屏 preload 超时（非 loading 结束现场 load） |
| 展示跳过·概率未命中 | enter/back 概率 |
| 展示跳过·AB 面 | B 专属位在 A 面 |

多位广告解读时，§2.5 行数 **≥ 9 × 广告位数**（可合并同路径须在表中注明，合并项 ≤ 20%）。

### 1.11.6 与 §2.11 的关系

- 每个广告位在 §2.12 写**业务时序专表**（预加载→展示→成功/失败）
- 若广告位链路含多阶段（如开屏：UMP→preload→loading 结束查缓存→show/skip→展示回调补货），须在 §2.11 有对应专节（P 编号可与广告清点表对应）
- **禁止**只在 §2.2 写「Splash 展示开屏」而不展开 UMP 后 preload、loading 结束无缓存 skip、跨页缓存、重试与补货

### 1.11.7 禁止的偷懒写法（广告）

- ❌ 「插屏有缓存就展示」（无预加载时机、无无缓存分支、无补货）
- ❌ 「启动页展示开屏广告」（无 UMP→preload→loading 结束查缓存、无 skip 后缓存存活、无跨页保留）
- ❌ 「开屏 load 超时跳页」当作 loading 结束现场 load（标准链路禁止现场 load）
- ❌ 只列 8 个 AdSense 名称，不写各自动作时机、**重试**、**补货**
- ❌ 展示成功/失败合并成「正常展示」
- ✅ §2.12 每位一张小表：预加载时机 | 展示时机 | 成功处理 | 失败处理 | **load 重试** | **补货**（分行列举函数名）

### 1.11.10 AB 面中途切换与广告展示专则（**项目已接入 AB 面且涉广告时强制**）

目标：产品、开发、测试能回答——**「preload 在 A 面、show 前已升 B，这则广告还能不能展示？升 B 后 B 专属位会不会突然有货？」**

#### 1.11.10.1 适用范围

满足**全部**即触发（无 AB 面则整节标「不涉及」并说明依据）：

- 工程存在 `commitAbFace` / `currentIsModeB()` / `applyByMode` / `AbSettlementCoordinator` 等 AB 面能力
- 本次解读/接入范围含广告 preload、show 或 `AdRemoteConfigManager`

#### 1.11.10.2 必查项（写 §2.12 / §2.5 前强制）

| # | 检查项 | 须写清 |
|---|--------|--------|
| A1 | **preload 时刻面别** | 触发 preload 时 `isModeB`、`canShowAd`、B 专属位、`ad_config_a/b` 分别是什么 |
| A2 | **show 时刻面别** | show 前是否再判闸门；与 preload 时刻不一致时走哪条 |
| A3 | **中途 A→B** | `commitAbFace` / 阶段2 `applyModeUpdateAfterExtended` / `onFirebaseFetchCompleted` 补升 B 发生在 **preload 后、show 前** 时：已缓存广告 **继续 show / discard / 重 load** 哪条 |
| A4 | **升 B 后仍可展示** | A 面 preload 的缓存，升 B 后 consume 是否合法；unit/sense 是否错配 |
| A5 | **B 专属位** | A 面 skip 的 B 位，升 B 后是否 **补 preload** 或可 show |
| A6 | **配置刷新** | `applyRemoteConfigCore(true)` 后已缓存广告是否 invalidate |

#### 1.11.10.3 §2.5 最低场景（有 AB 时**至少**）

| 场景 | 说明 |
|------|------|
| preload@A → 未升 B → show@A | 常规路径 |
| preload@A → 升 B → show 前 | 有缓存 / 无缓存各 1 行 |
| B 专属 preload skip@A → 升 B | 是否补 preload / 首次 show |
| show 进行中升 B | 若可达须写用户感知与回调 |

#### 1.11.10.4 §2.13 隐患

须纳入收益隐患类型 **`面别切换错配`**：preload 与 show 面别/JSON 不一致 → 无展示、错误 unit、discard 浪费请求。

#### 1.11.10.5 与 §1.6.5 的关系

§1.6.5 写 **load/preload 调用瞬间**配置竞态；本节写 **缓存生命周期 + 面别变更后 show 合法性**，二者均必填，不可互相替代。

### 1.11.11 开屏广告标准链路验收专则（**涉开屏 / LOADING_SPLASH 时强制**）

目标：验收开屏是否为 **「UMP 预加载 → loading 结束查缓存 → 有则 show、无则 skip」**，且 **缓存跨开屏页存活**。

#### 1.11.11.1 标准时序（验收基准）

```mermaid
flowchart TD
    UMP[UMP 完成] --> PRE[preload 开屏]
    PRE --> LOADING[启动页 loading 动画 并行]
    LOADING --> END{loading 动画结束}
    END -->|takeCached 有货| SHOW[展示开屏]
    END -->|takeCached 无货| SKIP[不展示 进下一跳]
    PRE --> LATE[preload 可能晚于 loading 结束]
    LATE --> CACHE[成功仍写入 Loader 缓存]
    SKIP --> CONT[preload/重试 仍继续]
    SHOW --> REPL[展示回调触发 补货 preload 下一条]
    DESTROY[开屏页 finish/destroy] --> KEEP[Loader 缓存保留 不 discard]
```

#### 1.11.11.2 硬性规则

| 规则 | 要求 |
|------|------|
| **禁止 loading 结束现场 load** | loading 结束瞬间无缓存 → **仅 skip**，**不得** `loadAd` / 现场 `Fetcher.load` 阻塞等待 |
| **UMP 后须 preload** | 开屏 preload 须在 UMP（或 consent）完成后触发 |
| **展示绑 loading 结束** | show 判定须绑在 **loading 动画结束**（或等价放行点），非 UMP 结束或 AB commit |
| **skip 不终止 preload** | 无缓存 skip 后，已在途 preload **继续**；失败走 §1.11.12 重试 |
| **跨页缓存存活** | 开屏 Activity `finish`/`onDestroy` **不得** destroy 已 load 未 consume 的实例；**禁止**「开屏页没了广告也没了」 |
| **晚到仍入缓存** | preload 在 loading 结束或进主页后才成功 → Loader **须有**开屏缓存，供下次或后续路径使用 |
| **与 AB 交叉** | loading 结束至 show 间若 A→B，走 §1.11.10 |

#### 1.11.11.3 标准链路对照表（§2.12 LOADING_SPLASH **必填**）

```markdown
| 步骤 | 标准预期 | 源码实际 | 一致？ | 代码锚点 |
|------|----------|----------|--------|----------|
| UMP 后 preload | 是 | | | |
| loading 结束查缓存 | 仅 takeCached | | | |
| 无缓存 | skip，不现场 load | | | |
| skip 后 preload 继续 | 是 | | | |
| 页 destroy 后缓存保留 | 是 | | | |
| 晚到 load 成功入缓存 | 是 | | | |
| 展示回调即补货 | 是（§1.11.13） | | | |
```

任一行「不一致」须在 §2.12 写**偏差说明**与对用户/收益的影响；接入验收时视为**待修复项**（除非用户明确接受偏差）。

#### 1.11.11.4 §2.5 开屏最低场景

除 §1.11.5 外，开屏位**至少**覆盖 §1.11.5 表中「开屏·*」四行 + UMP 未完成不 preload。

### 1.11.12 广告 load 失败重试专则（**涉广告时强制 · 每位必填**）

目标：**每一个** AdSense 位在 load/preload **失败**后都有可追踪的重试机制；源码无则标 **缺失/待接入**。

#### 1.11.12.1 每位必写（§2.12 专表一行）

| 必写项 | 要求 |
|--------|------|
| **失败触发** | `onAdFailedToLoad`、超时、`NO_FILL`、闸门导致的 fail 是否计入 |
| **重试次数** | 上限、是否按 sense 区分 |
| **重试间隔** | 固定 ms / exponential backoff / 与 `canRequest` 关系 |
| **重试锚点** | 函数/类；谁 schedule（协程、`Handler`、Loader 内部） |
| **耗尽后** | 停止请求 / 仅等补货 / 打日志与埋点 |
| **与 skip 关系** | 开屏 loading 已 skip 后，重试成功是否仍入缓存（**须是**） |
| **与补货关系** | 补货 load fail 是否走同一重试链 |

#### 1.11.12.2 §2.5 最低场景（每位至少 2 行）

- load 失败 → 重试 → 成功入缓存  
- load 失败 → 重试耗尽  

#### 1.11.12.3 接入验收

解读/接入交付时，广告位清点表「load重试锚点」列**不得为空**；空则写「**缺失/待接入**」并在 §2.13 标 **高** 隐患。

### 1.11.13 广告补货专则（**涉广告时强制 · 每位必填**）

目标：**每一个** AdSense 位在 **consume/show 后**都有补货 preload，保证「用完就有下一则 load 在途或入缓存」。

#### 1.11.13.1 通用规则（全部 adType，含 Banner 等价路径）

| 必写项 | 要求 |
|--------|------|
| **补货触发** | 展示成功回调：`onImpression`、`onAdDismissedFullScreenContent`、`bind` 成功等（以源码为准） |
| **补货函数** | `AdReplenishCoordinator` / `preloadAd` / `replenishOn*` / Loader 内部 |
| **补货时机** | **展示回调触发即 load**（用完即加载）；不得仅依赖「下次进页才 preload」 |
| **补货失败** | 走 §1.11.12 重试 |
| **skip 无 show** | 开屏 skip 无缓存时，仍须有 **preload/重试** 保证后续有货（与 §1.11.11 跨页缓存一致） |

#### 1.11.13.2 开屏专规（LOADING_SPLASH）

| 规则 | 要求 |
|------|------|
| **触发点** | **开屏广告触发展示回调**（如 `onImpression` 或 SDK show 成功回调）**立即** preload 下一条 |
| **语义** | **用完就加载**——consume 与补货 preload **同一展示周期内**启动，不等到下次冷启 |
| **与 skip 区分** | 未 show 的 skip **不触发**「用完补货」，但须仍有 UMP 后 preload + 失败重试使缓存 eventual 有货 |

#### 1.11.13.3 §2.12 / §2.13

- 清点表「补货锚点」列每位必填  
- R# 表须含 **补货** 类型行（每位至少 1 行）  
- 缺补货 → 隐患 **`consume 无补货`**，等级 **高**

#### 1.11.13.4 禁止的偷懒写法

- ❌ 「展示后会补货」不写回调名与函数  
- ❌ 开屏只在「下次启动」才 preload  
- ❌ 只写插屏补货，开屏/原生/Banner 省略  
- ✅ 「`onImpression` → `AdReplenishCoordinator.replenishOnSplash` → 非阻塞 preload 下一条」

### 1.11.8 广告预加载阻塞分析专则（**接入/解读广告或涉广告 Skill 时强制**）

目标：产品、开发、测试能回答——**「这次 load 会不会卡住用户 / 卡住下一则广告 / 拖慢启动？」**  
凡解读广告预加载、接入广告 Skill、或实现方案涉及 `preload`/`load` 编排时，**必须**完成本节分析；**凡存在阻塞必须显式说明**，禁止用「后台预加载」一句带过。

#### 1.11.8.1 适用范围

满足**任一**即触发（与 §1.11 叠加，不替代）：

- 用户要求**接入**广告相关功能、广告 Skill、MonetizationKit / PromoCore 预加载编排
- 解读 `StartActivity`、`AdPreloadCoordinator`、`preloadAd`/`preloadAdAwait`、`OverlayPromoFetcher.load`、`InlinePromoFetcher.load`、`LaunchPromoFetcher.load` 等
- §1.5 扫描命中 `preload*` + `suspend`/`await`/`launch`/`async` 组合
- 同一入口**连续预加载 ≥2 个**不同 `AdSense` / `PromoPlacement` 类型

#### 1.11.8.2 必须先判定的三类问题（写正文前强制）

| # | 问题 | 须写清 |
|---|------|--------|
| Q1 | **只加载一个广告时，这次 load 是否阻塞？** | 调用栈是否 `suspend` 直到 `onAdLoaded`/超时；阻塞**调用方协程**还是仅阻塞**当前 Activity 跳转** |
| Q2 | **同协程内连续 preload 两个不同类型，是否阻塞？** | 例：`load(A)` 完再 `load(B)` → **串行阻塞**；须写总耗时上界（各超时相加？共享 deadline？） |
| Q3 | **多个 preload 分别 launch，是否阻塞？** | 火忘 `launch` 通常**不阻塞**调用方，但须写：展示时是否 `await` 缓存、SDK 是否隐性串行、是否占主线程 |

#### 1.11.8.3 阻塞判定口径（统一用语）

| 类型 | 代码特征 | 解读须写 |
|------|----------|----------|
| **阻塞式 preload** | `suspend fun load` + 调用方 `await`/顺序调用无 `launch`；或 `withTimeout { load }` 在关键路径上 | 阻塞**谁**、最长等多久 ms、超时后**下一跳**（直跳/降级/仍展示其它位） |
| **非阻塞 preload** | `scope.launch { load }` 且调用方不 `join`/`await` | 写清「调用方立即继续」；展示侧是否仍可能 `takeCached` 等待 |
| **半阻塞** | UMP 后 preload 开屏但不 await AB commit；preload 火忘但 loading 结束 show **仅** takeCached | 须写 **§1.6.5 竞态** + §1.11.10 面别切换 + 「不阻塞 A，但阻塞 B」三连（见 §1.7.6） |
| **隐性串行** | 多个 `launch` preload 但共用单线程 Dispatcher 或 AdMob SDK 内部队列 | 标 `(待验证)` 并说明验证方式；**禁止**写「并行 preload 互不影响」而无依据 |

#### 1.11.8.4 预加载阻塞清点表（写 §2.12 / 方案前**强制**）

```markdown
### 预加载阻塞清点（解读/接入前）

| 序号 | 触发点（页面/函数） | AdSense/位 | 调用方式 | 是否阻塞 | 阻塞对象 | 超时 ms | 超时后行为 | 对展示/收益影响 |
|------|---------------------|------------|----------|----------|----------|---------|------------|-----------------|
| L1 | … | … | suspend/launch/串行 | 是/否/半阻塞 | 跳转/协程/用户点击 | … | … | 例：阻塞跳转→展示率降；非阻塞→可能无缓存直跳 |
```

- 清点表**每一行 preload/load** 都要有；同函数内**连续两行**须注明是否「上一行未完成则下一行不开始」
- **只加载一个广告**：单独一行，**禁止**合并进「批量预加载」
- **对广告收益的影响**列必填：阻塞导致晚 show / 超时 discard / 串行导致第二位永不 preload 等

#### 1.11.8.5 常见编排模式（解读时须对照源码确认，禁止照搬）

| 模式 | 典型代码形态 | 阻塞结论（须源码验证） |
|------|--------------|------------------------|
| 启动页开屏 **UMP 后 preload** | UMP 后 `preloadAd` / `LaunchPromoFetcher.load`（非 loading 结束） | **通常非阻塞**调用方；loading 结束**仅** takeCached（§1.11.11） |
| 启动页开屏 **loading 结束 show** | loading 动画结束 `takeCached` → show / skip | **不**现场 load；无缓存 skip |
| 语言页后 **插屏 show** | `OverlayPromoFetcher.load` + `show` 在跳转前 | **阻塞**离开语言页（load+show 串在跳转协程内） |
| 启动 **批量 preload 列表** | `for (sense) { launch { preload } }` | 调用方**通常不阻塞**；须写 show 时是否仍有货 |
| 启动 **批量 preload 串行** | `for (sense) { preloadAwait(sense) }` | **阻塞**该协程直至全部完成或逐项超时 |
| 同协程 **两种类型连续 load** | `load(INTERSTITIAL); load(NATIVE)` | **串行阻塞**该协程；写总耗时与第二位是否常被跳过 |
| show 前 **takeCached 为空** | 不阻塞 preload，但 show 时同步失败 | 归类为**展示路径无缓存**，非 preload 阻塞，但仍须写 |

#### 1.11.8.6 与 §2.12 / §2.5 的关系

- §2.12 每位「预加载时机」行须引用阻塞清点表对应 **L#**
- §2.5 至少增加场景：**阻塞 preload 成功**、**阻塞 preload 超时**、**非阻塞 preload 但 show 无缓存**、**同协程串行第二位未执行**（有则必填）
- 接入广告 Skill 的**实现方案**（非仅解读）亦须附 §1.11.8.4 清点表或等价表，**未经用户确认阻塞验收不得改 preload 编排**

#### 1.11.8.7 禁止的偷懒写法

- ❌ 「预加载在后台进行，不阻塞 UI」（未说明 show/跳转是否 await）
- ❌ 「StartActivity 会预加载多种广告」（未说明串行/并行、是否阻塞放行闸）
- ❌ 只写 `preloadAd` 函数名，不写 **suspend 还是 launch**
- ✅ 「`StartActivity.preloadX` 在 `lifecycleScope.launch` 内 **非阻塞**调用方；但 `goNext` 前 **await** 语言插屏 `load+show`，**阻塞**进引导页最多 N ms」

### 1.11.9 广告请求全链路确认清单（**接入广告功能 / 涉广告 Skill 交付时强制**）

目标：接入或解读广告后，用户能**逐项确认**——**每一次**向 SDK 发起 load/request 的**时机在哪**、**是否预加载点**、**是否阻塞**、**该位置有无浪费请求或损害展示率的隐患**。

#### 1.11.9.1 适用范围（命中任一即必填 §2.13）

- 用户要求**接入**广告功能、广告 Skill、MonetizationKit / PromoCore / AdMob 变现
- 声称「广告已接入完成」「预加载已接好」的**实现交付**（含 Bug 修复改 preload 编排）
- 解读**全链路广告**（启动页 → 各页 preload/show → 补货）
- §1.5 扫描命中 `trackRequest` / `load(` / `loadAds` / `preload` / `Fetcher.load`

**不适用**：用户只问单个广告位展示 UI、与 load/request 无关的纯文案——仍遵守 §1.11，可省略 §2.13。

#### 1.11.9.2 「请求时机」定义（须穷尽清点）

凡会导致 **AdMob SDK 发起 load**（或等价 `ad_request` 埋点）的代码路径，均算一条**请求时机**，**至少**覆盖：

| 请求类型 | 典型含义 | 须单独成行 |
|----------|----------|------------|
| **预加载请求** | 为后续 show 提前 load，结果进缓存 | 是 |
| **现场 load 请求** | show 前或 show 同时才 load（**Banner**；开屏 **不属于** loading 结束现场 load） | 是 |
| **展示前补 load** | `takeCached` 空后再 load（若有） | 是 |
| **曝光后补货请求** | `AdReplenishCoordinator` / 展示成功后 preload 下一条 | 是 |
| **条件性批量预加载** | 启动页 `for` 循环 preload 多位 | **每位各一行**，禁止合并成「启动预加载多种」 |
| **重复/续作请求** | 热启动、回前台、RC 刷新后再 preload | 有则必填 |

**检索要求**（解读/接入交付前须执行或等价搜索）：

```bash
rg "trackRequest|\.load\(|loadAds|preload|Preload|Fetcher\.load" --glob "*.kt"
rg "ad_request|PromoMetricsSink" --glob "*.kt"
```

遗漏任一 `Fetcher.load` / `Loader.load` 调用点 → **清单不合格**。

#### 1.11.9.3 主清单模板（**须输出给用户确认**，不得只在内心自检）

```markdown
## 📋 广告请求全链路确认清单（§1.11.9 · 请逐项确认）

> 说明：下列每一行 = 代码中**一次**会向 SDK 发起 load 的路径（或等价 ad_request）。  
> **预加载点**列：是 = 主要为缓存；否 = 现场 load；补货 = 曝光后补 load。

| R# | 请求类型 | 请求时机（生命周期/用户动作） | 代码位置 | AdSense/位 | 预加载点 | 是否阻塞式请求 | 阻塞对象 | 闸门/条件 | 收益隐患等级 | 隐患说明（对展示率/eCPM/浪费请求） |
|----|----------|------------------------------|----------|------------|----------|----------------|----------|-----------|--------------|-----------------------------------|
| R1 | 预加载 | 例：冷启 UMP 后 | `StartActivity.xxx` | LOADING_SPLASH | 是 | 是/否/半阻塞 | 放行闸/跳转 | canShowAd+… | 高/中/低/无 | 例：无条件 preload→用户永不进该页→请求无展示 |
| R2 | … | … | … | … | … | … | … | … | … | … |

### 预加载点汇总（仅列「预加载点=是/补货」的行）

| R# | 位 | 触发时机 | 是否阻塞 | 与 show 的时间关系 | 隐患摘要 |
|----|-----|----------|----------|-------------------|----------|

### 阻塞式请求汇总（仅列「是否阻塞式请求≠否」的行）

| R# | 位 | 阻塞谁 | 最长等待 ms | 超时后行为 | 对用户体验/展示率影响 |
|----|-----|--------|-------------|------------|----------------------|

### 收益隐患汇总（等级 ≥ 中 或 你标记须关注的项）

| R# | 隐患类型 | 说明 | 建议（仅方案，不改代码除非用户确认） |
|----|----------|------|--------------------------------------|
| H1 | 过早/无效请求 | … | … |
| H2 | 请求远大于展示 | … | … |

**隐患类型参考**（择一或多选，须写具体原因）：

- `过早请求`：用户尚不可能到达 show 场景（如未选语言却 preload 语言插屏）
- `无效请求`：闸门必 false 仍 load（UMP 未过、A 面 B 位、Premium）
- `请求无展示`：load 成功但 discard/超时未 show / 用户路径跳过
- `条件不匹配`：预加载条件与展示条件不一致（如全量 preload vs 条件 show）
- `串行阻塞拖慢`：阻塞导致 show 窗口错过或第二位永不 load
- `重复请求`：同进程同位短时间多次 load 无 consume
- `竞态错配`：commit 前/后配置不一致导致 unit 或 sense 错配
- `面别切换错配`：preload@A 与 show@B（或升 B 后 discard/闸门变化）导致无展示或错误 unit（§1.11.10）
- `consume 无补货`：展示 consume 后无 preload 下一条（§1.11.13）
- `开屏跨页丢缓存`：开屏页 destroy discard 实例，或 skip 后永不入缓存（§1.11.11）
- `重试缺失`：load fail 无重试链（§1.11.12）
- `热启动浪费`：热启重复 preload 已 discard 的缓存

### 请你确认

1. 上表是否**穷尽**当前工程所有广告请求时机？（有遗漏请指出 R# 或文件）
2. **预加载点**与**阻塞式请求**判断是否准确？
3. **收益隐患**等级与说明是否认可？需调整哪几行？
4. 回复「**清单确认**」或逐条修正；**清单确认前**，不得声称广告接入已完成。

```

#### 1.11.9.4 硬性要求

1. **一行一请求路径**：同一函数内连续 load 两种类型 → **两行**（R# 连续），并注明是否串行阻塞（链到 §1.11.8 **L#**）。
2. **预加载点不得漏**：凡 `preload` / 缓存意图的 load，「预加载点」列标 **是** 或 **补货**；开屏 UMP 后 preload 标 **是**；Banner 现场 show 标 **否**；**开屏 loading 结束禁止现场 load**（§1.11.11）。
3. **阻塞必须显式**：「是否阻塞式请求」只能填 **是 / 否 / 半阻塞**；填「是/半阻塞」须在「阻塞对象」列写清阻塞谁。
4. **收益隐患必填**：每一行至少填 **等级 + 一句话**；确实无隐患填 **无** 并写依据（如「show 条件与 preload 条件一致且 consume 路径已接」）。
5. **停止点**：输出 §2.13 清单后 **必须停止**，等待用户「清单确认」；与 `@ai_work` 门禁并行——**未经确认不得改 preload/请求编排**（用户明确要求除外）。
6. **交付广告接入时**：Implementation Report / 实现说明书**必须**附 §2.13 或等价表格，且自检 §2.10 对应项已勾选。

#### 1.11.9.5 与 §1.11.8 的关系

- §1.11.8 **L#** 表专注 preload/load 的**阻塞机制**；§1.11.9 **R#** 表覆盖**全部请求时机**（含非 preload）+ **用户确认** + **收益隐患**
- 每个预加载点应在 R# 表中有对应行，并可在「预加载点汇总」再汇总一次
- 阻塞式请求应在 R# 与「阻塞式请求汇总」双重出现，避免用户漏看

#### 1.11.9.6 禁止的偷懒写法

- ❌ 「启动页会预加载广告」（无 R# 表、无文件行号、无位数）
- ❌ 只列 AdSense 名称，不列**请求时机**
- ❌ 隐患只写「可能影响收益」不填等级与机制
- ❌ 声称「已接入完成」但未输出清单或未获「清单确认」
- ✅ 输出完整 R# 表 + 三类汇总 + 明确请用户确认

---

## 1.6 全场景枚举方法（必须先于 §2.5 / §2.7）

解读时**禁止只举「典型 3 例」**；须根据代码穷尽本功能**可能出现的所有场景**。

### 1.6.1 场景从哪里来（必须逐条核对）

1. **§2.3 分支表**：每个叶子分支 → §2.5 至少 **1 行场景**（可合并同结果但须注明）
2. **网络维度**：正常 / 弱网（高延迟、丢包）/ 无网（离线）——仅当该功能**等网络或可能受影响**时各写场景
3. **数据维度**：有数据 / **无数据·空串·null** / 仅缓存·过期缓存 / 半写入·标志位不一致
4. **超时维度**：每个 `withTimeoutOrNull`、deadline、重试上限 → **各写**超时内成功、超时后 fallback 场景
5. **异常维度**：catch、`Failed`、SDK 错误码、Class 加载失败、Activity 不存在等代码里有的兜底
6. **路径维度**：冷/热、fastPath/skip、首次/重复调用、防重 return、debounce 命中
7. **续作维度**（§2.9 + §2.11）：首次结论 + 各阶段逐步机制 + 续作成功修订 + 续作失败仍保持 + 无变化
8. **远程配置维度**（§1.8）：功能若读 Firebase RC → **按 FC 全量取值维度**写清（存在性 + 代码约定取值 + 其它异常值样板 + 拉取/cache），**禁止**只罗列源码里出现的 `if` 分支而漏写「未配置」「约定值以外」等可达情况（见 §1.8.0）
9. **组合场景**：分支可组合时写**可达组合**（如「弱网 + FC 8s 超时用缓存 + 子项 key 未配默认开启 + 买量升 B」），不写代码不可达组合

### 1.6.2 场景分类标签（每行必标）

`正常` `弱网` `无网` `超时` `异常` `无数据` `边界` `竞态` `续作修订` `远程配置` `Debug`（无则不打该标签）

### 1.6.3 完备性要求

- §2.5 行数 **≥** §2.3 独立分支数；若少须说明合并理由
- **禁止**用「等」「类似」「其它情况类似」代替未列出的分支
- 代码无法触达的分支标「不可达」并说明；不可达的不强行编造

### 1.6.4 超时点清单（写 §2.2 / §2.4 / §2.5 之前**强制**）

解读前在源码中搜索本功能相关文件的 `withTimeoutOrNull`、`TIMEOUT_MS`、`deadline`、`awaitFc`、`fetchActivate`，整理为**超时点清单表**（可放在 §2.3.1 表 B 或 §2.2 前附表），**每个超时点至少 2 个场景**（未超时成功 + 超时后 fallback）：

| 超时点（代码锚点） | 阈值 ms | 超时后行为 | 是否阻塞用户路径 | §2.4 须有边 | §2.5 至少场景 |
|-------------------|---------|------------|-----------------|------------|--------------|
| （逐项填写，不得漏项） | | | | | |

**AB 面 / 启动 / 广告类功能**，清单**至少**核对下列超时（有则必填，无则标「本功能不涉及」）：

| 超时点 | 典型锚点 | 阈值 | 超时后 |
|--------|----------|------|--------|
| Firebase **fetch** | `runFcFetchAndApply` + `withTimeoutOrNull(FC_FETCH_TIMEOUT_MS)` | **8000** | `fetchOk=false`，**仍** `onFirebaseFetchCompleted`，用**缓存 RC** |
| 判面**等 FC** | `awaitFcReadyForMode2` | **30000** | 强制 `fcReady=true`，用**当前 RC 缓存**执行 Mode2 |
| 归因阶段1 | `AttributionManager` + `referrerTimeoutMs` | 默认 **6000** | 可能进阶段2 阶梯重试 |
| 归因阶段2 阶梯 | `RETRY_SCHEDULE_MS` | 30s/2m/5m/14m | 单次拉 refer；15min 终局 |
| 开屏 preload | UMP 后 `LaunchPromoFetcher.load` / preload | **10000**（若有） | preload 超时：loading 结束 takeCached 无货→skip；**不**在 loading 结束现场 load；已 load 实例**不** destroy |
| bootstrap 等待 | `AdPreloadCoordinator.awaitBootstrapAndSdkReady` | **30000** | 按 A 面放弃 B 位预加载 |

**禁止**：时序表/流程图写了「T3 拉 Firebase 8s」却**不画** 8s 超时分支；**禁止**只写「等 FC 30s」却不写超时后「用缓存继续判面、不永久卡住」。

### 1.6.5 并行竞态与「配置生效时刻」（涉启动/AB/广告时**强制**）

多协程并行时，须写清**结论取决于哪一刻哪条分支已执行**，至少覆盖：

| 竞态维度 | 须写明的分支 | 禁止的误导简写 |
|----------|-------------|----------------|
| AB commit vs 开屏 preload | UMP 后 preload **不 await** `configAppliedThisProcess`；preload 瞬间读 `AdRemoteConfigManager` **当前**配置 | ❌「开屏必定用 A 默认」 |
| loading 结束 show | **仅** `takeCachedAd`；无缓存 skip，**禁止**现场 load | ❌「loading 结束会等 load 完」 |
| commit **前** | `effectiveModeB=false`；`applyDefaultLocalAssetsA` + FC 后 `ad_config_a` 路径 | ❌「已经按 B 请求开屏」 |
| commit **后**（含 B） | `commitAbFace` → `applyRemoteConfigCore(true)`，开屏可能用 **B 方案** JSON | ❌「开屏永远不走 B 配置」 |
| UMP 耗时 | UMP 慢时 AB/FC 更可能先于 `loadSplash` 完成 | ❌忽略 UMP 对竞态的影响 |
| `currentIsModeB()` vs 开屏 | 开屏位**非** B 专属，看 `enableFor`；B 专属预加载看 `isModeB` | ❌混为「开屏也要 canShowAd」 |

**准确表述模板（解读边界/与启动页关系时优先使用）**：

> 与启动页：**不专门等待** AB `commitAbFace` 再放行；开屏在 UMP 结束后 **preload**，loading 结束 **仅查缓存** show/skip——preload **调用瞬间** `AdRemoteConfigManager` 已生效配置（commit 前多 A，commit 后可能 B）；**并非每次必定 A**。中途 A→B 对已缓存/show 的影响见 §1.11.10。

§2.5 须至少有 **2 行竞态场景**（commit 前开屏 / commit 后开屏，或 UMP 快 vs 慢）。

---

## 1.7 代码锚点双语标注（产品 + 开发共读，**强制**）

解读中出现的**每一个**类名、方法名、关键标志位，须同时给出**中文业务含义**。代码锚点保留原文，便于开发对照源码；业务描述用产品能懂的语言。

### 1.7.1 写什么（三段式）

对每个锚点写清：

| 字段 | 写给谁 | 要求 |
|------|--------|------|
| **代码锚点** | 开发 | 类/方法/常量，保持原文，如 `AbSettlementCoordinator.startSettlement` |
| **一句话业务含义** | 产品+开发 | 这一步**在业务上做什么**（≤25 字），禁止只复述类名 |
| **用户/系统效果** | 产品 | 完成后用户或系统状态有什么变化（可选一行） |

**反例（禁止）**：节点写 `startSettlement`、表格写 `commitAbFace` 就结束。  
**正例**：`startSettlement` → **「冷启动时启动 AB 面结算（归因+远程配置并行）」** → 完成后尝试锁定 A/B 面。

### 1.7.2 流程图节点格式（§2.4 必须遵守）

**每张流程图之前**，必须先有 **「流程图名词说明」** 表格（覆盖图中全部代码锚点）：

| 代码锚点 | 是什么（业务） | 谁调用/何时触发 |
|----------|----------------|-----------------|

流程图**节点内**采用「代码 + 业务」双语，推荐 mermaid 写法：

```mermaid
flowchart TD
    R["AppAdsBootstrap.run<br/>广告与AB面初始化入口"]
    S["startSettlement<br/>冷启AB面结算协调器"]
    R --> S
```

规则：
- 节点第一行：**代码锚点**（可缩短为 `类.方法`）
- 节点第二行：**中文业务含义**（不用代码术语堆砌）
- 判断菱形：主文案用**业务问题**（如「是否热启动？」），副行可附 `hotResumeFastPath`
- 超时边标注：`6s超时→先按自然量A面提交` 而非仅 `timeout`

### 1.7.3 时序表与场景表

- **§2.2「动作」列**：格式 `代码锚点 — 业务含义`，两列亦可拆成「代码锚点 | 业务含义」
- **§2.5「走的路径」列**：须写 `业务步骤链（代码锚点链）`，例如：`拉安装来源（AttributionManager.init）→ 判 B 面（Mode2Utils.evaluateMode2）`
- **§2.7「走的路径」**：先业务动词链，括号内附函数名

### 1.7.4 子功能展开（§2.8）

每个子功能小节标题格式：**`{业务名}（{代码锚点}）`**，如 **归因拉取安装来源（AttributionManager）**。

### 1.7.5 自检

- 产品同事仅读流程图+名词表，能否说出主流程在干什么？不能则不合格。
- 开发同事能否从名词表反查到具体类/方法？不能则不合格。

### 1.7.6 准确表述铁律（禁止产品误解，**强制**）

与 `@code-term-bilingual` 一致；功能解读额外要求：

1. **条件先于结论**：先写 `if 标志位/时刻`，再写结果；禁止无条件的「总是/必定/永远」。
2. **「不阻塞」三连写**：不等谁 → 自己仍等什么 → 对方完成后是否续作改数据。
3. **配置类名词**：`AdRemoteConfigManager` 须解释为「**当前内存中生效的广告 JSON**」，并说明何时被 `applyByMode` / `applyDefaultLocalAssetsA` 覆盖。
4. **反例入库**（解读时不得复犯）：

| 错误表述 | 正确表述 |
|----------|----------|
| 开屏不等待 AB commit | 开屏 UMP 后 preload；loading 结束仅 takeCached | loading 结束**当时**未 commit 则 show 判定按 A 闸门；已 commit 为 B 则按 B；preload 瞬间配置见 §1.6.5 |
| 未判面一定是 A 体验 | 未 commit 前对外 `currentIsModeB()` 为 false；**开屏配置**仍取决于 commit 是否已完成 |
| AB 不阻塞启动页 | 不阻塞 **等待 commit**；启动页仍等 UMP；开屏 preload 与 AB **竞态**见 §1.6.5；loading 结束 **不**现场 load |

---


## 1.8 远程配置（Firebase RC / 广告 RC）场景维度（**涉 RC 时强制**）

功能若通过 `FirebaseRemoteConfig`、`fetchActivate`、`exists(key)`、`AdRemoteConfigBridge`、`NotificationRemoteConfig` 等读写远程参数，**除 §2.3 分支表外**，必须增加 **§2.3.1 远程配置专表** 与 §2.5 中对应的 `远程配置` 场景行。

### 1.8.0 FC 全量写法 vs 代码分支写法（**写 RC 前必读**）

解读 RC 时，目标是写出 **Firebase 侧所有可达情况** 对功能的影响，**不是**把源码里写了几条 `if` 就列几条。

| 写法 | 含义 | 要求 |
|------|------|------|
| ❌ **代码分支枚举** | 只写 `==0`、`==1` 等显式判断 | **不合格**——易漏「未配置」「约定值以外」 |
| ✅ **FC 全量取值维度** | 按 key 的**存在性 × 取值语义**穷尽 | **合格**——覆盖代码未单独 `if` 但运营可能配出的情况 |

**每个影响结论的 RC key，§2.3.1 表 A 须覆盖下列维度（按源码核对后填结果，无则标「本 key 不涉及」）：**

| 维度 | 说明 | §2.5 场景行 |
|------|------|------------|
| **① key 未配置** | Firebase **没下发过**该 key（`exists==false` / `VALUE_SOURCE_STATIC`） | **必写 1 行**（可与代码默认语义合并叙述，但须单独可辨） |
| **② 已配置 · 代码约定取值** | 源码**显式处理**的每个取值（如 `=0`、`=1`、枚举字符串等） | **每个约定值各 1 行** |
| **③ 已配置 · 约定值以外** | 运营配了 key，但取值**不在**代码约定集合内（如总开关 `=22`） | **只举 1 个样板**即可（如「=22（代表任意非 0 非 1）」），**禁止**穷举 2、3、22… |
| **④ 拉取/cache 生命周期** | fetch 成功/进行中/超时、仅缓存、commit 前后 apply 等 | 见 §1.8.2，与 key 取值**分表分场景** |

**样板举例规则（维度 ③）**：

- 表 A「已配置其它值」列：写 **1 个代表值 + 括号说明**即可，例如：`22（任意非0非1，走代码子项判面，同未强制）`
- §2.5：该类场景 **1 行**，场景名含「其它异常值」或「非约定取值」，正文引用样板值并说明**与哪一类语义相同或走哪条兜底**
- **禁止**为每个非法数字各写一行；**禁止**因源码无 `==22` 就省略「约定值以外」维度

**读源码时**：先找约定取值集合与兜底路径，再反推维度 ①③ 的默认语义是否与 ② 中某值相同；相同可在表 A 注明「与 =X 同路径」，但 §2.5 仍建议保留 ① 独立一行（产品需区分「没配」与「配错」）。

**禁止**把下列四类情况混为一谈（产品易误解，须在报告中用不同场景编号或表 A 分行区分）：

| 情况 | 代码典型判断 | 业务含义（产品语言） | 写法要求 |
|------|-------------|---------------------|----------|
| **A. key 未在 RC 中配置** | `exists(key)==false` | Firebase **没下发过**该 key，走 App **内置默认** | 维度 ①，**必写** |
| **B. key 已配置 · 约定关闭值** | 如 `exists && value==0` | 运营**明确关闭**（以源码为准） | 维度 ② |
| **C. key 已配置 · 约定开启值** | 如 `exists && value==1` | 运营**明确开启**（以源码为准） | 维度 ② |
| **D. key 已配置 · 约定值以外** | 如 `exists && value∉{0,1}` | 异常/误配/灰度，走**兜底分支**（须读源码） | 维度 ③，**仅 1 个样板** |

### 1.8.1 必须先读清的代码问题（写场景前自问）

解读 RC 相关功能时，**必须先回答**（答案写入 §2.3.1；按 **§1.8.0 全量维度**，不只答代码里有的 `if`）：

1. **key 是否存在**怎么判？（如 `FirebaseRemoteConfig.getValue(key).source != STATIC`）
2. **key 不存在**（维度 ①）时，代码默认当「开」还是「关」还是「走另一套默认」？
3. **代码约定取值集合**（维度 ②）有哪些？每个取值的业务语义？
4. **约定值以外**（维度 ③）走哪条兜底？与 ① 或 ② 中哪一类**同路径**？（表 A 用 **1 个样板值**写即可）
5. **总开关**与**子项开关**优先级？
6. 判面/拉配置前是否**等 FC 就绪**？等多久？超时后用**缓存**还是**本地 assets 默认**？
7. FC **晚于**首次 commit 时，是否有**补升/补降**续作？（见 §2.9）
8. 广告 JSON 类 RC 与布尔/数值类 key 的 apply 顺序？

### 1.8.2 远程拉取生命周期（须覆盖的场景）

凡有 `fetchActivate` / `fetchAndActivate` / 协调器 FC 轨，§2.5 **至少**覆盖：

| 拉取状态 | 触发 | 典型代码行为 | §2.5 须写 |
|----------|------|-------------|-----------|
| 未拉取 | 进程刚启动 | 用 STATIC/本地默认 + Application 先行 A 面 assets | 1 行 |
| fetch **进行中** | FC 轨 `runFcFetchAndApply` | AB 轨可能已在 `awaitFcReadyForMode2` 轮询 | 1 行 |
| fetch **成功** | `fetchOk=true` | `fcReady=true` → `onFirebaseFetchCompleted` → apply 远程 | 1 行 |
| fetch **8s 超时** | `withTimeoutOrNull(FC_FETCH_TIMEOUT_MS)` 返回 null | `fetchOk=false`，**仍**设 `fcReady=true` 并 `onFirebaseFetchCompleted`，日志「使用缓存 RC」 | **必写** ≥1 行；§2.4 **必画**超时边 |
| **等 FC 30s 超时** | AB 轨 `awaitFcReadyForMode2` 轮询截止 | 强制 `fcReady=true`，「使用当前 RC 缓存执行 Mode2」，**不永久卡住** | **必写** ≥1 行；与 fetch 8s 超时**分场景** |
| 仅缓存/离线 | 无网或从未 fetch 成功 | 缓存 key 存在性与取值分别写；判面读 `exists(key)` | 与「key 未配」区分 |
| commit **前** FC 完成 | `onFirebaseFetchCompleted` 且 `!configApplied` | `effectiveModeB=false`，按 **A 面** apply 广告/通知 RC | 1 行 |
| commit **后** FC 完成 | 已 `commitAbFace` | 按当前 `isModeB` apply；总开关=1 可补升 B | 续作行 |
| 拉取后修订 | `naturalModeBIfMasterSwitchOverridesCommitted` | 与已 commit 不一致时补判（B 永久锁定后不可降 A） | 续作行 |

**Firebase 超时双轨（AB 面必读，§2.4 流程图必须体现）**：

```mermaid
flowchart LR
    FCstart[FC轨开始] --> Fetch{fetch 8s内成功?}
    Fetch -->|是 fetchOk=true| FCdone[fcReady=true]
    Fetch -->|否 超时| FCcache[fetchOk=false 用缓存RC]
    FCcache --> FCdone
    FCdone --> OnComplete[onFirebaseFetchCompleted apply配置]
    ABwait[AB轨 awaitFcReady] --> Wait{30s内 fcReady?}
    Wait -->|是| Mode2[resolveNaturalModeBAfterFc]
    Wait -->|否 超时| Mode2cache[强制fcReady 用缓存判面]
    Mode2cache --> Mode2
```

解读时须在正文中用**产品语言**说明：FC 8s 超时 ≠ 判面失败；AB 轨最多再等 30s，超时仍用缓存判面并 commit，**不阻塞启动页开屏**。

### 1.8.3 videodownload · AB 面 / Mode2 远程 key 对照（解读 AB 面时**必须**核对）

以下为当前工程 `Mode2Utils` / `AbSettlementCoordinator` 的**定稿语义**（其它功能有 RC 时按同样格式自建表，不得照搬数值）：

#### 总开关 `enable_mode2_with_video`

| key 状态 | 取值 | 判面结果 |
|----------|------|----------|
| **未配置**（维度 ①） | — | **不强制** A/B → 进入代码子项判面 |
| 已配置（维度 ②） | `1` | **强制 B**（`writeMode2Resolved(true)`） |
| 已配置（维度 ②） | `0` | **强制 A**（自然 B 已锁定则保持 B） |
| 已配置（维度 ③ · **样板 1 个即可**） | 如 `22`（代表任意非 0 非 1） | **不强制** → 与「未配置」在判面路径上相同，进入代码子项判面；日志会打「非0非1」 |

> 维度 ③ **不必**再写 =2、=3 等行；表中 `22` 仅为样板，解读时说明「凡 ∉{0,1} 的已配置值均走此兜底」。

#### 代码子项（须**分别**写场景，禁止合并成「无代码子项」）

| key | 业务含义 | **未配置**（不存在） | **已配置 =0** | **已配置 =1** |
|-----|----------|---------------------|---------------|---------------|
| `enable_installation_source_condition` | 是否检查安装来源/referrer | **视为开启**，参与检查 | **关闭**，不参与 | 参与检查 |
| `enable_installed_from_google_play_condition` | 是否检查 Google Play 安装 | **视为开启**，参与检查 | **关闭**，不参与 | 参与检查 |

#### 子项组合后的兜底（`evaluateMode2`）

| 子项参与情况 | 业务含义 | 结果 |
|-------------|----------|------|
| **两 key 均未配置** | 默认**两项检查都开** | 按 referrer+GP **正常判**（买量+GP 可 B） |
| **两 key 均已配置且均为 0** | 运营**主动关闭全部检查** | `hasApplicableCheck=false` → **默认 A** |
| 有子项参与且任一失败 | 如 organic、侧载 | **A** |
| 有子项参与且全部通过 | 买量+GP | **B** |

**命名规范（§2.5 场景名称）**：

- ✅ `RC子项均未配置→默认开启检查→按来源判`
- ✅ `RC子项均已配置且=0→无检查参与→默认A`
- ❌ `无代码子项`（未说明是「未配置」还是「配 0」，**禁止**）

#### Firebase 拉取与等待（AB 面 **§2.2 / §2.4 / §2.5 必填**）

| 阶段 | 代码锚点 | 阈值 | 超时/失败行为 | 是否阻塞启动页 |
|------|----------|------|---------------|----------------|
| FC fetch | `AbSettlementCoordinator.runFcFetchAndApply` | **8000ms** | `fetchOk=false`，`logAllConfigs(_timeout)`，**仍** `onFirebaseFetchCompleted` | 否（与 AB 轨并行） |
| 等 FC 就绪 | `awaitFcReadyForMode2` | **30000ms** | 强制 `fcReady=true`，用 RC **缓存**执行 `resolveNaturalModeBAfterFc` | 否（不阻塞 `StartActivity` 放行闸） |
| FC 完成后 apply | `onFirebaseFetchCompleted` → `applyRemoteConfigCore` | — | 未 commit：`effectiveModeB=false` 按 A apply；已 commit：按 `isModeB` | 开屏不等 commit |

#### 其它常见广告 RC（启动/广告解读时核对）

| key / 配置 | 作用 | 未配 / 超时时的默认（须读 `AdRemoteConfig` 源码填写） |
|------------|------|------------------------------------------------------|
| `referrer_timeout_ms` | 归因阶段1 超时 | 默认 **6000ms**（limit 对象兜底） |
| `ad_config_a` / `ad_config_b` | 按面别广告 JSON | 未 commit 用 A assets；FC 超时用缓存 + 本地 assets 兜底 |

### 1.8.4 §2.5 远程配置场景最低行数（涉 RC 时）

按 **§1.8.0 全量维度** 核对，**不是**按源码 `if` 个数：

- **维度 ①**：每个影响结论的 key → **至少 1 行**「未配置」
- **维度 ②**：源码中**每个约定取值** → **各 1 行**（如 =0、=1）；若「未配置」与某约定值兜底相同，表 A 可注明，但 §2.5 仍保留 ① 独立一行
- **维度 ③**：若存在「约定值以外」兜底 → **仅 1 行样板**（如 `总开关=22→走代码子项`），**禁止**穷举多个非法数字
- **维度 ④**（拉取/cache）：至少 **fetch 成功 + fetch 超时用缓存** 2 行；有 `awaitFc*` 再加 **等 FC deadline 行**
- **续作**：FC 完成后与 commit 不一致的补升/补降，各 1 行

### 1.8.5 产品可读总结句（解读 RC 功能时建议附在 §2.3.1 末尾）

用一两句话帮产品区分（可按功能改写）：

> **「Firebase 没配这个 key」≠「配成了 0」≠「配成了约定以外的数（如 22）」。**  
> 没配 → 内置默认；配 0/1 → 运营明确关/开；配 22 等异常值 → 走兜底（解读时举 **1 个样板** 说明即可，不必逐个数字举例）。

---

## 1.9 链接目录与快速导航（**强制，解决长文难找**）

报告与对话正文**过长**时，读者不应从顶滚到底。生成解读时**必须在正文最前**放可点击的 **§2.0 链接目录**，并为关键章节、场景编号提供锚点跳转。

### 1.9.1 放哪里

| 载体 | 要求 |
|------|------|
| **对话回复（主交付）** | 第一个一级标题下、§1 解读范围之前或之后，紧跟 **§2.0 目录**；**必须含完整正文**，不得只有镜像备份路径 |
| **镜像备份 .md** | 元数据注释后、正文第一个可见章节即为 **§2.0 目录**；内容与对话**同级完整**；路径见 §5.2 |

### 1.9.2 标题与锚点规范（保证链接可点）

- 各级标题**必须带章节号**：`## 1. 解读范围`、`### 2.1 功能身份与作用`、`#### S12：…`
- **禁止**用纯数字列表代替标题（否则无法锚定）
- 目录链接使用 Markdown 锚点，与标题自动 slug 对齐，格式：`- [2.1 功能身份与作用](#21-功能身份与作用)`
- 中文标题：去掉空格；`§`、`.` 保留或按预览器 slug 规则（Cursor/GitHub 一般为小写+连字符）；**生成后自检链接能跳到对应标题**
- 场景编号统一锚点：标题写 `#### S12：阶段1超时先A` → 目录写 `[S12 阶段1超时](#s12阶段1超时先a)`（与渲染器 slug 一致即可）
- 若预览器锚点不稳定，在关键节前插入 HTML 显式锚点：`<a id="sec-25"></a>` 紧挨 `## 2.5 …` 上一行（**仅镜像备份 md** 可用；对话内优先标准 Markdown 标题锚点）

### 1.9.3 §2.0 目录模板（必须按此结构输出）

```markdown
## 2.0 目录

**一句话**：{本功能 1 句摘要}

### 快速阅读（按角色）

| 角色 | 建议跳转 |
|------|----------|
| 产品 | [2.1 作用](#21-功能身份与作用) → [2.3 分支](#23-分支与判断逻辑) → [2.4 流程图](#24-流程图) → [2.7 重点场景](#27-全场景逐项说明) |
| 开发 | [2.2 时序](#22-实现步骤与时序) → [2.11 分阶段](#211-分阶段详细说明与它有关必写) → [2.3.1 远程配置](#231-远程配置专表)（若有）→ [2.12 广告位专表](#212-广告位专表涉广告时必填)（若有）→ [2.13 请求全链路清单](#213-广告请求全链路确认清单)（涉广告）→ [2.6 走读](#26-关键实现走读) |
| 测试 | [2.5 场景矩阵](#25-全场景矩阵) → [2.7 逐场景](#27-全场景逐项说明) → [2.12 广告位专表](#212-广告位专表涉广告时必填)（涉广告）→ [2.13 请求全链路清单](#213-广告请求全链路确认清单)（涉广告）→ [2.10 自检](#210-输出前自检) |
| 产品/接入验收 | [2.13 请求全链路清单](#213-广告请求全链路确认清单) → 确认预加载点、阻塞项、收益隐患 → 回复「清单确认」 |

### 全文目录

- [1. 解读范围](#1-解读范围)
- [2.0 目录](#20-目录)（本节）
- [2.1 功能身份与作用](#21-功能身份与作用)
- [2.2 实现步骤与时序](#22-实现步骤与时序)
- [2.3 分支与判断逻辑](#23-分支与判断逻辑)
- [2.3.1 远程配置专表](#231-远程配置专表)（无 RC 则删此行）
- [2.4 流程图](#24-流程图)
- [2.5 全场景矩阵](#25-全场景矩阵)
- [2.6 关键实现走读](#26-关键实现走读)
- [2.7 全场景逐项说明](#27-全场景逐项说明)
- [2.8 递归子功能](#28-递归子功能)
- [2.9 异步续作](#29-异步续作与结论修订)
- [2.11 分阶段详细说明](#211-分阶段详细说明与它有关必写)（**有阶段/子轨时必填**）
- [2.12 广告位专表](#212-广告位专表涉广告时必填)（**涉广告时必填**）
- [2.13 广告请求全链路确认清单](#213-广告请求全链路确认清单接入涉广告时必填)（**接入/解读广告时必填，须用户确认**）
- [3. 双视角](#3-双视角)
- [2.10 输出前自检](#210-输出前自检)

### 场景速查（§2.5 有 Sxx 时必填）

按分类列出跳转（每类至少链到该类第一个 Sxx；场景多时可链全部 Sxx）：

| 分类 | 跳转 |
|------|------|
| 正常 | [S01 …](#s01…) · [S02 …](#s02…) |
| 远程配置 | [S04 …](#s04…) |
| 超时 | [S12 …](#s12…) |
| 续作 | [S14 …](#s14…) |
| … | … |
```

### 1.9.4 长文减负规则（与目录配合）

- **§2.7 逐场景**：场景 **>12 条** 时，对话与镜像备份 md 均须在 §2.0 提供「场景速查」；§2.7 内每个 `#### Sxx` 标题**不得省略编号**
- **§2.11 分阶段**：**不得**为减负而省略阶段专节；阶段2 等续作阶段**禁止**只在 §2.7 用「同续作」代替（见 §1.10）
- **§2.5 矩阵**：保留完整表格；§2.7 可对结果完全相同的场景合并叙述，但**阶段相关 Sxx 须能在 §2.11 找到逐步说明**
- **产品快速通道**：§2.0 的「产品」路线指向的章节，须能在 **3 分钟内**读懂主流程，不强制先读 §2.7 全部
- **禁止**只有目录没有正文；**禁止**有正文没有目录

### 1.9.5 自检

- 从 §2.0 点任一链接能跳到对应节？
- 产品同事能否只点「快速阅读·产品」三四个链读完主流程？

---

## 2. 输出结构（必须按此顺序，**先目录后正文**）

章节顺序：`2.0` → `2.1`～`2.8` → `2.11`（有阶段时）→ **`2.12`（涉广告时）** → **`2.13`（接入/解读广告且须用户确认请求全链路时）** → `2.9`（有续作时）→ `3` → `2.10`

### 2.0 链接目录（**必须第一块**，见 §1.9）

- 含 **一句话摘要**、**按角色快速阅读**、**全文目录**、**场景速查**（有 Sxx 时）
- **对话（主交付）与镜像备份 md 内容一致**；禁止对话精简、文件完整

### 2.1 功能身份与作用

| 项 | 内容 |
|----|------|
| 业务作用 | **为什么要有这个功能**（解决什么问题） |
| 用户可感知效果 | 用户什么时候能感觉到它 |
| 后台职责 | 数据/状态/配置层面的变化 |
| 上游 | 谁调用、什么条件下触发 |
| 下游 | 完成后触发谁 |
| 是否阻塞关键路径 | 若适用：是否阻塞启动页/主流程等；**不阻塞≠不等待其它步骤**；并行须写 §1.6.5 竞态 |

### 2.2 实现步骤与时序（串行 / 并行）

用表格列出 **T0 → Tn**：

| 步骤 | 代码锚点 | 业务含义（产品可读） | 串行/并行 | 线程/协程 | 前置条件 | 完成后状态 |
|------|----------|---------------------|-----------|-----------|----------|------------|

要求：
- **「业务含义」列必填**；禁止动作列只有函数名（见 §1.7）
- 明确标出**可与其它步骤并行**的项
- 异步点写清：挂起点、恢复点、回调/协程 resume 时机
- **必须分两行路径**（若存在 §2.9 续作）：
  - **主路径（阻塞/首次结论）**：用户当下能感知的时间线
  - **续作路径（非阻塞/可能修订）**：首次结论之后的后台任务

### 2.3 分支与判断逻辑（代码翻成文字）

**每一个** `if/else`、`when`、状态机转移都要有业务含义。用**分支表**或**伪代码**（禁止只写「根据配置判断」）：

| 条件（业务语言） | 条件（代码等价） | 结果 | UI/用户感知 |
|----------------|-----------------|------|-------------|

若判断依赖子模块 → 在 §2.8 递归展开，本节仍须复述**结论**。

### 2.3.1 远程配置专表（**功能涉 RC 时必填**）

在 §2.3 之后、§2.4 之前插入。表格须让**产品能单独阅读**此节即知「没配 / 配了约定值 / 配了异常值 / 拉取超时」会怎样。

**写法**：按 **§1.8.0 FC 全量取值维度** 填表，**禁止**只填源码里出现的 `if` 分支。

**表 A：RC key 存在性与取值语义**（每个影响结论的 key 一行；维度 ③ 列只写 **1 个样板值**）

| RC key | 业务含义（产品） | ①未配置 | ②约定取值（逐项，如 =0 / =1） | ③其它值（样板 1 个，如 =22） | 代码锚点 |
|--------|-----------------|--------|-------------------------------|------------------------------|----------|

**表 B：远程拉取与 cache**（维度 ④，有 fetch 时必填）

| 阶段 | 代码锚点 | 业务含义 | 超时 ms | 超时/失败后行为 | 用户是否感知 |
|------|----------|----------|---------|----------------|-------------|

**表 C：RC 与首次结论/续作**（有 commit 后补判时必填）

| 时机 | RC 状态 | 首次结论 | 续作是否改结论 | 修订锚点 |
|------|---------|----------|----------------|----------|

解读 **AB 面**时，表 A **必须**包含 §1.8.3 中总开关 + 两个子项 key，且**分开写**「子项均未配置」与「子项均=0」两行场景。

### 2.4 流程图（必须）

标题须为 `### 2.4 流程图`（供 §2.0 目录锚定）。**先写「流程图名词说明」表（§1.7.2），再画图。**

mermaid `flowchart TD` 或 `stateDiagram-v2`；**必须包含**：

1. 正常路径  
2. 弱网 / 高延迟  
3. 无网 / 离线  
4. **每一个超时点**（标具体 ms + **超时后业务行为** + **用什么数据继续**：缓存/本地默认/占位）；须对照 §1.6.4 清单**逐项有边**，无超时则写「无专用超时」  
5. 异常 / 失败兜底  
6. **涉 Firebase 时**：fetch 成功、fetch 8s 超时、awaitFc 30s 超时 **三条边至少各 1 条**（可画在主图或 FC 子图）  

**节点标注（强制）**：
- 每个节点：`代码锚点` + 换行 + `中文业务含义`（见 §1.7.2 示例）
- 禁止图中出现**仅有代码名、无业务说明**的节点
- §2.9.3 续作专图同样遵守本规则

### 2.5 全场景矩阵（**穷尽，非典型抽样**）

标题须为 `### 2.5 全场景矩阵`。矩阵**上方**可加一行：「逐条说明见 [§2.7](#27-全场景逐项说明) | 场景速查见 [§2.0](#20-目录)」。

**本表须覆盖 §1.6 所列全部可达场景**；一行一场景，用编号 `S01`、`S02`…

| 编号 | 分类标签 | 场景名称 | 触发条件（可复现） | 输入/数据状态（**含 RC：key 未配/=0/=1/cache**） | 超时阈值 | 走的路径（业务步骤 + 代码锚点） | 功能结果 | UI/用户感知 | 后台状态变化 | 后续/续作 |
|------|----------|----------|-------------------|---------------|----------|-------------------|----------|-------------|--------------|-----------|

填写要求：
- **正常**：每个主要成功分支至少 1 行
- **弱网 / 无网**：凡依赖网络的步骤，三种网络各至少 1 行（不适用则写「本步骤不等网络」并说明）
- **超时**：每个超时点至少「未超时成功」+「超时后 fallback」2 行（无超时则 1 行标「无专用超时」）
- **异常**：每个 `catch` / `Failed` / 错误 return 1 行
- **无数据**：空 referrer、空 MMKV、null、未 commit、标志位 false 等**代码出现过的空态**各 1 行
- **边界**：如刚好等于 deadline、重复调用第二次、debounce 内二次触发
- **续作**：§2.9 每条续作链路的成功/失败/无变化各 1 行
- **远程配置**（§1.8）：涉 RC 时按 **§1.8.0 全量维度 + §1.8.4** 最低行数；「未配置」「约定值以外（样板 1 个）」**不得**因源码无对应 `if` 而省略；场景名称**禁止**含糊的「无代码子项」，须写明「未配置」或「=0」或「其它值如=22」
- 有常量必须写 **ms** 或 **RC key 名 + 取值**；禁止「可能变慢」「按配置」等空泛词

矩阵末尾附：**场景计数** `共 N 场，其中 正常 a / 弱网 b / 无网 c / 超时 d / 异常 e / 无数据 f / 续作 g / 远程配置 h`

### 2.6 关键实现走读（不必通读整文件）

- 摘录与本功能判断直接相关的代码（每段建议 ≤ 40 行）
- 格式：

```
【摘录】路径 → 类/方法
【解读】逐行/逐段业务含义（简体中文）
【读完后应知道】2～3 个结论
```

### 2.7 全场景逐项说明（与 §2.5 一一对应）

标题须为 `### 2.7 全场景逐项说明`。节首可加：** [← 回目录](#20-目录)** · **[← 场景矩阵](#25-全场景矩阵)**

**§2.5 每一行 `Sxx` 在本节须有对应小节**（禁止只挑 3 个「典型」）。每个场景标题**必须**用四级标题且带编号，供 §2.0 场景速查链接：

```markdown
#### S12：阶段1超时先公布A [{弱网}]
```

#### 单场景模板（每个 Sxx 复用）

```markdown
#### Sxx：{场景名称} [{分类标签}]

- **触发条件**：…
- **前置数据**：…（含空/无/缓存；**涉 RC 时写明各 key：未配/=0/=1/仅缓存**）
- **执行顺序**：串行步骤 / 与谁并行（用业务动词，括号附代码锚点）
- **走的路径**：业务步骤链 → …（`类.方法` 链）
- **结果**：功能输出 + 全局状态字段变化
- **用户看到**：…
- **后台实际**：…
- **若失败/超时/续作**：…（无则写「无续作」）
- **与其它场景差异**：相对 Syy 的一点关键区别（一行）
```

允许将**结果完全相同**的 Sxx 合并为「S03/S04 同路径」，但须在 §2.5 注明合并，且合并项 ≤ 总场景数 20%。

**禁止**：只写「例如」「典型情况包括」而不覆盖 §2.5 全部编号。

### 2.8 递归子功能（有则必写）

父功能依赖的判断逻辑，若由其它类实现 → **每个子功能**用小节说明。

小节标题格式：**`{业务名}（{代码类名}）`**。正文须含：业务作用（产品语言）+ 核心分支 + 代码锚点。

拆解深度：直到可用业务语言完整复述；叶子节点标「叶子」及原因。

**与 §2.11 的关系**：§2.8 写**子模块职责与核心分支**；若子模块内含**多个阶段**（如 `AttributionManager` 的阶段1/阶段2），各阶段**逐步细节**放在 §2.11，§2.8 可摘要并链到 §2.11 对应专节。**禁止**只在 §2.8 用一段话概括阶段2。

### 2.11 分阶段详细说明（与它有关必写）

标题须为 `### 2.11 分阶段详细说明`。当 §1.10 阶段清点表有 **≥1 行**时，本节**必填**；放在 **§2.8 之后、§2.9 之前**（先讲各阶段怎么做，再讲续作对照与总图）。

节首须附：**阶段清点表**（§1.10.2，可与 §1 重复但不得省略），并写：

> 以下逐节对应清点表中每一阶段/子轨；**与它有关就要详细说明**，不单列矩阵一行带过。

#### 专节标题格式

```markdown
#### P3：归因·阶段2 阶梯重试（AttributionManager.runScheduledRetryLoop）
```

- 标题含 **业务阶段名 + 代码锚点**
- 清点表有几项，本节至少几个 `####` 专节（合法合并见 §1.10.2）

#### 单阶段模板（§1.10.3 十项，每项一段或一表行，禁止合并成一段）

```markdown
#### Px：{阶段名}（{代码锚点}）

**1. 阶段身份**：…
**2. 启动时机**：…
**3. 启动条件**：…（布尔 + MMKV/枚举）
**4. 不启动条件**：…
**5. 执行步骤**：

| 步序 | 业务动作 | 代码锚点 | 完成后状态 |
|------|----------|----------|------------|

**6. 超时与重试**：…（多阶梯用下表）

| 阶梯/次数 | 触发时刻 | 单次超时 | 成功分支 | 失败/超时分支 |
|-----------|----------|----------|----------|---------------|

**7. 成功时**：标志位 / 回调 / 对外效果
**8. 失败/超时/空数据时**：…
**9. 与上下游衔接**：上一阶段 → 本阶段 → 下一阶段
**10. 用户是否感知**：…
```

**阶段2 类续作**：须额外满足 §1.10.4 各必写块（进入判定、标志位表、阶梯逐步表、跨启动恢复、修订限制、子流程图）。

#### 自检

- 用户只读 §2.11，能否回答「阶段2 什么时候进、每一步干什么、15min 后怎样」？
- 开发能否按 §2.11 逐步对照源码，无需再猜 `needsExtendedReferrerRetry` 何时 true？

### 2.12 广告位专表（**涉广告时必填**）

标题须为 `### 2.12 广告位专表`。当 §1.11 适用时，本节**必填**；放在 **§2.11 之后、§2.9 之前**（或紧接 §2.3.1 之后，若解读以广告为主且无多阶段子轨）。

节首须附：**广告位清点表**（§1.11.4），并写：

> 以下逐位写清**预加载时机、展示时机、展示成功处理、展示失败处理**；禁止只列广告位名称。

#### 总表：按 AdSense 枚举（每位必填）

对每个 `AdSense`（或用户点名的单位）用**独立四级标题**：

```markdown
#### AdSense.LOADING_SPLASH（开屏）

**类型策略**：开屏｜UMP preload → loading 结束仅查缓存（§1.11.11）｜展示回调即补货（§1.11.13）

| 维度 | 内容 |
|------|------|
| **预加载时机** | UMP 后 preload；与 loading 并行；skip/destroy 后仍继续；**阻塞清点 L#** |
| **标准链路对照** | §1.11.11.3 表（预期 vs 源码）**必填** |
| **预加载是否阻塞** | **必填**：UMP 后 preload 是否阻塞；loading 结束 show **不得**阻塞现场 load |
| **展示时机** | **loading 动画结束** → takeCached；有则 show / 无则 skip |
| **展示成功** | 展示回调 → **立即**补货 preload 下一条；埋点/跳页 |
| **展示失败/ skip** | 无缓存 skip；preload+重试继续；跨页缓存保留 |
| **load 重试** | §1.11.12：失败次数/间隔/锚点 |
| **补货** | §1.11.13：`onImpression`/show 回调 → preload 下一条 |

**预加载→展示→补货 简图**（建议 mermaid，至少含 UMP→preload→loading 结束→show/skip→展示回调补货）
```

#### 每位「展示失败」最低分支清单（对照源码勾选）

解读时在本位小节末尾用勾选列表自检（无则标「本位的无此分支」）：

```
- [ ] UMP / isInit / isSubs 闸门未过
- [ ] AB 面 B 专属位在 A 面
- [ ] A 方案 allowsAdSense 未包含该位
- [ ] 日配额 canRequest / canShow 满
- [ ] 无缓存（插屏/原生/开屏 loading 结束）
- [ ] 开屏 loading 结束**禁止**现场 load（§1.11.11）
- [ ] 开屏页 destroy 后 Loader 仍保留缓存
- [ ] load 失败重试（§1.11.12：次数/间隔/锚点）
- [ ] load 超时（preload 路径，非 loading 结束现场 load）
- [ ] SDK show 失败 / ad_no_show
- [ ] 展示回调后补货 preload（§1.11.13）
- [ ] AB 中途 A→B 与缓存/show（§1.11.10，有 AB 时）
- [ ] 概率未命中（enter/back）
- [ ] 非前台展示拦截
```

#### 跨位共性（可选一节，避免重复）

若多位共用逻辑（如 enter/back 共用 `AdNavigationCoordinator`），可另设 `#### 插屏类共性（InterAdLoader + AdNavigationCoordinator）`，但 **§2.12 各 AdSense 行仍须保留**，可写「详见共性节」并**至少**写该位独有触发时机（如底栏 interval）。

#### 自检

- 产品能否回答：「用户点返回时，有缓存/无缓存分别怎样？」
- 测试能否按表写出：预加载在哪个 Activity 哪行逻辑触发？
- 是否每位都写了**展示回调后**补货（§1.11.13；开屏=用完即 load）？
- 是否每位都写了 **load 失败重试**（§1.11.12）？
- 开屏是否填 **§1.11.11.3 标准链路对照表**？

### 2.13 广告请求全链路确认清单（**接入/涉广告解读时必填 · 须用户确认**）

标题须为 `### 2.13 广告请求全链路确认清单`。当 §1.11.9 适用时，本节**必填**；放在 **§2.12 之后、§2.9 之前**。

节首须写：

> 以下为 **§1.11.9** 完整清单；**请你确认**所有请求时机、预加载点、阻塞式请求、收益隐患是否准确。**回复「清单确认」前，不视为广告接入验收通过。**

正文须包含（不得删减块）：

1. **R# 主表**（每一 SDK load / ad_request 路径一行）
2. **预加载点汇总**
3. **阻塞式请求汇总**（无则写「当前无阻塞式请求路径」+ 依据）
4. **收益隐患汇总**（无 ≥ 中 级则写「未发现」+ 依据）
5. **请你确认**（§1.11.9.3 模板四句）

**接入交付时**：用户任务是「接广告 / 改 preload / 涉广告 Skill」时，**§2.13 不得省略**；须获「清单确认」后再声称完成（用户明确跳过除外）。

#### 自检

- 用户能否仅读 §2.13 答：「几处 request？」「哪几处预加载？」「哪几处阻塞？」「哪几处浪费请求？」
- R# 行数是否覆盖源码独立 load/preload 调用点（合法合并 ≤20%）

### 2.9 异步续作与结论修订（**存在则必填，禁止省略**）

当 §1.5 扫描命中「多阶段 / 超时后续作 / 事后更新结论」时，**单独成章**（不可并入 §2.2 一行带过）。

**与 §2.11 分工**：§2.11 写**各阶段逐步机制**（含阶段2 全细节）；§2.9 写**首次结论 vs 修订对照**、跨阶段总览图、与主功能阻塞关系。**禁止**用 §2.9 代替 §2.11 的阶段2 专节。

#### 2.9.1 首次结论 vs 可能修订（对照表，必须）

| 维度 | 首次结论（何时锁定） | 异步续作（何时启动） | 修订触发条件 | 修订函数/锚点 | 修订后对外效果 | 用户是否感知 |
|------|---------------------|---------------------|--------------|---------------|----------------|-------------|

至少 1 行；有多条续作链路则多行。

#### 2.9.2 续作链路分步说明

对**每一条**续作链路写清（**逐步细节以 §2.11 为准**；本节写总览与对照，**不得**因本节已写而省略 §2.11 阶段专节）：

1. **启动时机**：在哪个函数、**首次 commit/return 之前还是之后**
2. **启动条件**：精确布尔条件（含 MMKV/标志位名）
3. **执行环境**：协程/线程、是否阻塞主流程
4. **超时与重试**：各阶段 ms、次数、失败后状态
5. **成功时**：更新哪些字段、调用哪些 `apply*` / `notify*`
6. **失败/仍超时时**：结论是否不变、标志位如何收尾
7. **与主功能关系**：图示或一句「不阻塞 X，但可能改变 Y」

#### 2.9.3 续作专用流程图（必须）

单独一张 mermaid，从「首次结论已生效」画到「续作结束」，含超时/失败/无变化分支。须附续作专用「流程图名词说明」表，节点遵守 §1.7.2 双语格式。

#### 2.9.4 本项目常见续作模板（对照检查，命中则写）

解读下列功能时**必须**检查是否涉及 §2.9：

| 功能域 | 易忽略续作 |
|--------|-----------|
| AB 面 / 归因 | **§2.11 须分别有阶段1、阶段2 专节**；阶段1 不确定 → commit 占位 → 阶段2 **30s/2m/5m/14m 阶梯**（逐步表）→ 15min 终局 → `applyModeUpdateAfterExtended`（仅 A→B）；详见 §1.10.4 |
| AB 面 / FC fetch | `runFcFetchAndApply` **8s 超时** → `fetchOk=false` 用缓存 → **仍** `onFirebaseFetchCompleted` |
| AB 面 / 等 FC | `awaitFcReadyForMode2` **30s 超时** → 强制继续 → `resolveNaturalModeBAfterFc` 用缓存判面 |
| AB 面 / FC 续作 | `onFirebaseFetchCompleted` → commit 后总开关=1 可补升 B；**自然 B 锁定后不可降 A** |
| 广告 bootstrap | `configApplied` 后 `schedulePreload*`、`notifyAdRemoteConfigRefreshed` |
| 广告 · 预加载 | `AdPreloadCoordinator.preloadAfterLoading` / 各页 `preloadAd`；须写触发条件与闸门 |
| 广告 · 展示成功 | 曝光后 `AdReplenishCoordinator` / `展示后补货` scene；`recordImpression` |
| 广告 · 展示失败 | 无缓存 `takeCached==null` 直跳；开屏 loading 结束 skip **不现场 load**；preload fail → 重试（§1.11.12） |
| 广告 · 开屏标准链路 | UMP preload → loading 结束查缓存 → 有 show / 无 skip；跨页缓存保留（§1.11.11） |
| 广告 · 补货 | 每位展示回调即补货；开屏 `onImpression`/show 成功 → preload 下一条（§1.11.13） |
| 启动页 vs AB | **不 await** `commitAbFace`；preload/show 时刻面别与中途 A→B 见 §1.11.10 |
| 启动页放行 | loading 结束查缓存 show/skip；**禁止** loading 结束现场 load |
| 任意 `*Coordinator` / `*Manager` | `init` 回调后的第二段 `launch` |

未涉及则写「经扫描，本功能无异步续作修订」，并说明依据（读了哪些文件、哪些关键词未命中）。

### 2.10 输出前自检（必须附在报告末尾）

```
- [ ] 已执行 §1.6 全场景枚举（分支/网络/数据/超时/异常/无数据/续作/远程配置）
- [ ] 涉 RC 时已按 **§1.8.0 全量维度**填 §2.3.1（①未配置 ②约定取值 ③其它值样板 ④拉取/cache）；「未配」「=0」「约定外样板」已区分
- [ ] §2.5 行数 ≥ 分支数，附场景计数分类
- [ ] §2.7 每个 Sxx 均有对应说明（或合法合并已标注）
- [ ] 非仅 3 个典型例；无「等/类似」省略未列分支
- [ ] 已执行 §1.5 易忽略链路扫描
- [ ] 已填 §1.10.2 **阶段清点表**；清点表每一行在 **§2.11** 有对应专节（或合法合并已注明）
- [ ] §2.11 每个阶段含 §1.10.3 **十项**；阶段2 类续作含 §1.10.4 必写块（判定/标志位/阶梯逐步/跨启动/修订限制/子流程图）
- [ ] **禁止**仅矩阵/§2.9 一笔带过阶段2；与它有关阶段均已详细说明
- [ ] §2.9 续作已填（对照表+总图）；且未用 §2.9 代替 §2.11
- [ ] §2.2 已区分主路径 vs 续作路径
- [ ] 弱网/无网/无数据/超时/异常均有独立场景行（适用时）
- [ ] 已填 §1.6.4 超时点清单；**每个**超时点 §2.4 有边、§2.5 有场景
- [ ] 涉 Firebase 时已写 fetch 8s 超时 + awaitFc 30s 超时（成功/失败各覆盖），未合并成一句
- [ ] 不存在空泛句而无触发条件/函数名
- [ ] 每张流程图前有「流程图名词说明」表；图中节点均为代码+业务双语（§1.7）
- [ ] §2.2 时序表含「业务含义」列；产品可读主流程
- [ ] §2.0 链接目录已输出（全文目录 + 角色快速阅读 + 场景速查）
- [ ] **对话已输出完整正文**（非「见镜像备份文件」）；md 文件仅为镜像备份
- [ ] 对话与镜像备份 md 均可通过目录跳转，不必全文滚动
- [ ] §1.6.5 并行竞态已写（涉启动/AB/广告时 commit 前/后至少各 1 场景）
- [ ] 无「必定/总是/永远」类误导简写；「不阻塞」已写三连（见 §1.7.6）
- [ ] 符合 `@code-term-bilingual` 双语与准确表述
- [ ] **涉广告时**已填 §1.11.4 **广告位清点表**；§2.12 每位写清**预加载时机、展示时机、展示成功、展示失败**（含无缓存/超时/SDK失败/闸门分支）
- [ ] **涉广告预加载时**已填 §1.11.8.4 **预加载阻塞清点表**；每位/每次 preload 已标明**阻塞式/非阻塞**、阻塞对象、超时；**凡阻塞均已显式说明**
- [ ] 已分析：单广告 load 是否阻塞；同协程多类型 preload 是否串行阻塞；多 launch 是否隐性串行
- [ ] 广告解读已区分四类策略（开屏 UMP preload+loading 查缓存 / 插屏只消费缓存 / 原生绑缓存 / Banner 现场 show）
- [ ] **开屏**已填 §1.11.11.3 **标准链路对照表**；loading 结束无缓存为 skip（非现场 load）
- [ ] 已写 skip/destroy 后 preload 继续、**跨页缓存保留**、晚到 load 仍入缓存
- [ ] **有 AB 面时**已写 §1.11.10（中途 A→B、升 B 后缓存是否可 show、B 专属补 preload）
- [ ] **每位**已写 **load 失败重试**（§1.11.12）；缺失已标「缺失/待接入」
- [ ] **每位**已写 **补货**（§1.11.13）；开屏=**展示回调触发**立即 preload 下一条
- [ ] 每位广告展示成功后是否补货（`AdReplenishCoordinator` / `preloadAd`）已写明
- [ ] **接入/解读广告时**已输出 §2.13 **广告请求全链路确认清单**（R# 穷尽请求时机 + 预加载点 + 阻塞 + 收益隐患）
- [ ] §2.13 每行已填预加载点 / 是否阻塞 / 收益隐患等级；已请用户「清单确认」（未确认未声称接入完成）
- [ ] 镜像备份已写入 **`{约束skill}/.cursor/{app名}/`**（不在被解读 Android 工程内建目录）
```

---

## 3. 双视角（必须）

| 用户看到的 | 后台发生的 |
|-----------|-----------|

---

## 4. 禁止事项

- **禁止流程图/时序表只写代码名词**：如仅写 `AppAdsBootstrap.run`、`startSettlement` 而不说明「这是什么、业务上干什么」（§1.7）
- 不要只列函数名或贴代码不解释
- 不要省略弱网/无网/无数据/异常（每项必须在 §2.5 有独立场景行，适用时）
- **禁止只写典型示例**：不得用 3 个例子代替全场景矩阵
- **禁止省略关联阶段详细说明**：阶段2、FC 轨、老用户快路径等只要在清点表或矩阵中出现，**§2.11 必须有专节**；不得「阶段2 会重试」一句带过（§1.10）
- **禁止用 §2.9 / §2.7 代替 §2.11**：续作对照表不能替代分阶段逐步说明
- 不要写「详见某某类」而不展开
- 不要编造未读到的逻辑；不确定标「待确认」
- **禁止只写镜像备份 md、不在对话里输出完整正文**（见 §5；文件是备份，不能替代聊天内容）
- **禁止一笔带过异步续作**：不得仅用「后台可能更新」「阶段2 会重试」代替 §2.9 必填内容
- **禁止只写首次结论**：超时/弱网场景必须写清「先给出什么结论」+「续作是否/何时改写」
- **禁止跳过 commit 之后的逻辑**：`tryCommit` / `commitFinal` / `FINAL` 之后的函数调用必须追踪
- **禁止只罗列源码 RC 分支**：不得因代码只 `if (==0/==1)` 就省略「未配置」「约定值以外」；后者用 **1 个样板**写清即可（§1.8.0）
- **禁止混淆远程 key「未配置」「配置为 0」「配置为约定以外」**：三者语义往往不同（§1.8.0、§1.8.3）；不得用「无代码子项」一笔带过
- **禁止穷举非法 RC 数字**：约定值以外维度 **1 行样板**即可（如 =22），不得写 =2、=3、=22 各一行
- **禁止 RC 场景只写总开关**：有子项 key 时须按 **§1.8.0** 写清每个子项的 ①未配 / ②=0 / ②=1 / ③其它值样板（如有兜底）及组合兜底
- **禁止长文无链接目录**：解读超过约 3 屏时必须有 §2.0；不得让读者只靠滚动查找（§1.9）
- **禁止遗漏任一代码超时点**：流程图/时序出现「拉 FC」「等 FC」时，**必须**同时写 fetch 超时与 await 超时后的 fallback；漏写 Firebase 超时视为解读不合格
- **禁止用「可能用缓存」代替具体场景**：须写清触发条件、阈值 ms、后续函数链与用户/后台效果
- **禁止误导性简写**：如「开屏必定 A 默认」「永远是 A 体验」——须按 §1.6.5 / §1.7.6 写 commit 时刻分支
- **禁止把并行竞态写成单一路径**：AB/FC/UMP/开屏须写清**谁先完成**对配置与面别的不同影响
- **禁止广告解读只列广告位**：涉广告时须 §2.12 逐位写预加载/展示/成功/失败，不得只写 8 个 `AdSense` 名称（§1.11）
- **禁止混淆 preload 与 show**：插屏/原生/开屏 loading 结束须写明「只消费缓存」；开屏 **禁止** loading 结束现场 load（§1.11.11）；Banner 除外
- **禁止开屏「页毁缓存亡」**：不得出现开屏 Activity destroy 后 discard 且进程内永无开屏缓存（§1.11.11）
- **禁止省略 load 失败重试**：每位须有重试链或标「缺失/待接入」（§1.11.12）
- **禁止省略补货**：每位须写展示回调后补货；开屏须「用完即 load」（§1.11.13）
- **禁止忽略 AB 中途升 B 对广告的影响**：有 AB 面时须写 §1.11.10，不得只写 §1.6.5 load 瞬间竞态
- **禁止省略展示失败旁路**：无缓存直跳、load 超时跳页、概率跳过、闸门 false 静默跳过等**必须**写清下一跳函数与用户感知
- **禁止省略预加载阻塞说明**：不得写「后台预加载」而不分析 suspend/await/串行；**凡阻塞必须写明阻塞谁、多久、对展示率/收益的影响**（§1.11.8）
- **禁止混淆非阻塞 preload 与 non-blocking show**：preload 火忘但 show 前 await 缓存，仍须在阻塞清点表标「半阻塞」
- **禁止省略广告请求全链路清单**：接入/解读广告后未输出 §2.13（请求时机 + 预加载点 + 阻塞 + 收益隐患）或未获「清单确认」就声称接入完成（§1.11.9）
- **禁止遗漏请求时机**：未 grep `load`/`preload`/`trackRequest` 就写「已预加载」；批量 preload 不得合并为一行

---

## 5. 双份交付：对话正文 + 镜像备份 md（必须执行）

> **用户意图**：AI 回复之后，要**两份**——**聊天里是完整解读正文**；**镜像备份 md 里是同内容的归档**。文件**不是**替代对话，只是方便留存、检索、转发。

### 5.0 双份对照（交付物是什么）

| 交付物 | 载体 | 角色 | 内容要求 |
|--------|------|------|----------|
| **主交付** | Cursor **对话回复** | 用户当场阅读 | §2.0 目录 + §1～§2 全部块，**完整、不缩写** |
| **备份** | **`{约束skill}/.cursor/{app名}/` 下的 .md** | 归档 / 按 App 归类 | 与对话正文**一致**；**固定写在约束 skill 的 `.cursor` 下**，不在被解读的 Android 工程内建目录 |

**合格**：对话里能直接读完；末尾另起一行提示备份路径。  
**不合格**：对话只写「详见镜像备份」或只给文件路径；或对话是精简版、完整版只在文件里。

### 5.1 核心原则

1. **先写对话，再写文件**：顺序固定为——先在 Cursor 对话中输出**完整正文**（**最前** §2.0 链接目录，其后 §1～§2 全部块，含 §2.5、§2.7、§2.9、§2.10）；主交付**不得**缩短为「见文件」「见附件」。
2. **再写镜像备份 md**：用 Write 工具把**同一份正文**写入 §5.2 规定目录；文件 **首屏可见** 即为 §2.0 目录。文件 = **镜像备份**，不是第二套更详版。
3. **禁止**三种偷懒：
   - 把完整正文只放进 md 文件，聊天只剩路径或摘要；
   - 对话写精简版、文件写完整版（**双份须同等级完整度**）；
   - 长文无 §2.0 目录，让读者从头滚到尾。

### 5.2 输出目录与文件命名

#### 输出目录（**强制，固定落在约束 skill**）

镜像备份 **一律** 写入 **「约束」skill 的 `.cursor` 目录** 下，按**被解读 App 的桌面展示名**分子文件夹；**禁止**在被解读的 Android 工程根目录或其它项目路径下新建 `{app名}/` 文件夹。

```
{约束skill}/.cursor/{app名}/{用户提问主题}_{yyyy}-{M}-{d}_{H}-{m}.md
```

| 占位符 | 含义 | 示例 |
|--------|------|------|
| `{约束skill}` | 本规则所在 skill 根目录 | `/Users/…/skill/约束` |
| `{app名}` | 被解读 App 桌面展示名（文件夹名） | `video`、`Nitro PDF Pro` |
| 文件名 | **不含** app 名前缀（文件夹已区分 App） | `广告功能_2026-6-17_15-8.md` |

**目录结构示例**：

```
skill/约束/
├── .cursor/
│   ├── rules/              ← 本规则
│   ├── buildTxt/           ← 历史备份（已废弃，可保留）
│   ├── video/              ← 桌面名 = video 的 App 解读归档
│   │   └── 广告功能_2026-6-17_15-8.md
│   └── Nitro PDF Pro/      ← 另一个 App
│       └── AB面功能_2026-6-16_19-1.md
```

**`{约束skill}` 判定**（写入前执行）：

1. 若本规则文件路径含 `/skill/约束/` → `{约束skill}` = 该路径中 `…/skill/约束` 的绝对路径
2. 若 workspace 内存在 `**/skill/约束/.cursor/rules/cursor-function_description.mdc` → 取该文件向上到 `约束` 目录
3. 若规则仅被 sync 到 Android 工程的 `.cursor/rules/`（workspace 内无 `skill/约束`）→ **仍写入 canonical 路径** `/Users/MacLuo/Desktop/D/working/shenzhen/skill/约束`（或向用户确认约束 skill 绝对路径）；**不得**改写到当前 Android 工程根目录

目录 `{约束skill}/.cursor/{app名}/` 不存在则 **Write 前创建**。

#### `{app名}`（文件夹名，**强制**）

指 App **安装后在手机/模拟器桌面（启动器）上显示的名称**，即用户肉眼看到的应用名——**不是** `applicationId`、包名、`rootProject.name` 或工程文件夹名。

**获取顺序**（解读前从当前 workspace **主模块**读取，命中即停）：

| 优先级 | 来源 | 说明 |
|--------|------|------|
| ① | `app/src/main/res/values/strings.xml` 的 `name="app_name"` | Android 默认桌面名（**首选**） |
| ② | `AndroidManifest.xml` 中 `<application android:label="…">` 或启动 Activity 的 `android:label` | 若 label 为 `@string/xxx`，须**解析到 strings 取值** |
| ③ | 非 Android 或上述不可读 | 向用户确认桌面展示名；暂不可确认时用 `未知App` 并在 §1 解读范围注明 |

**文件夹名规范化**（仅处理非法字符，**禁止截断 app 名**）：

- 非法字符 `/ \ : * ? " < > |` 替换为 `-`
- 首尾空格去掉；连续空白压成单个空格
- **中文、英文、空格均可保留**；**不得**为缩短路径而截断 `{app名}`

#### `{用户提问主题}`（文件名主体）

取自用户**当次原问题**，提炼为简短文件名（与对话里问的内容一致，便于按问题找回报告）：

- 去掉套话前缀：如「查看」「帮我」「分析一下」「讲清楚」等
- 保留核心功能名，**优先用用户原话**（中文可直接用，如 `AB面功能`、`启动流程`）
- 文件名非法字符 `/ \ : * ? " < > |` 替换为 `-`；首尾空格去掉；连续空白压成无或 `-`
- 过长时截断至 **≤40 字符**（截断后勿以 `-` 结尾；**仅截断主题，不截断 app 名**）

#### 时间戳与其它

- **时间戳**：写入时刻**本地时间**，格式 **`yyyy-M-d_H-m`**（24 小时制），**必须**与示例 `2026-5-17_10-25` 同结构：
  - 年-月-日 用 `-` 连接；**日**与**时**之间用 `_`；时-分 用 `-` 连接
  - 月、日、时、分：**不补前导零**（如 5 月写 `5` 非 `05`，9 分写 `9` 非 `09`；10 点、25 分照常两位）
  - 完整样例：`…/skill/约束/.cursor/video/广告功能_2026-6-14_19-5.md`
- **同分钟冲突**：在 `.md` 扩展名前加 `-2`、`-3`…，如 `…/skill/约束/.cursor/video/AB面功能_2026-5-17_10-25-2.md`

**命名示例（用户问题 → 路径）**

| 桌面 app 名 | 用户提问 | 生成路径（写入时刻示例） |
|-------------|----------|--------------------------|
| Nitro PDF Pro | 查看 ab面功能 | `…/skill/约束/.cursor/Nitro PDF Pro/AB面功能_2026-5-17_10-25.md` |
| video | 查看广告功能 | `…/skill/约束/.cursor/video/广告功能_2026-6-17_15-8.md` |
| PDF Reader | 讲清楚放行闸逻辑 | `…/skill/约束/.cursor/PDF Reader/放行闸逻辑_2026-12-1_18-0.md` |

> **旧路径** `skill/约束/.cursor/buildTxt/` **已废弃**（新解读一律写入 `skill/约束/.cursor/{app名}/`）。历史 buildTxt 可保留，不必迁移。  
> **禁止**在被解读 Android 工程内（如 `xvdownloader/video/`）创建镜像备份目录。

### 5.3 文件头部元数据（仅镜像备份 md 需要）

对话正文**不要**包这段 HTML 注释；**仅**写入镜像备份的 `.md` 文件头部：

```markdown
<!-- cursor-feature-interpret
generated: 2026-5-17 10:25:00
appName: {桌面展示 app 名，与文件夹名一致}
topic: {用户原问题全文或摘要}
outputDir: {约束skill}/.cursor/{app名}/
filename: {用户提问主题}_{yyyy}-{M}-{d}_{H}-{m}.md
anchors: {主要代码路径}
rule: .cursor/rules/cursor-function_description.mdc
role: backup（镜像备份，主交付在对话正文）
-->
```

### 5.4 对话末尾备份提示（必须，且仅此一句可简短）

完整正文输出完毕后，**另起一行**（不替代正文、不删减上文）：

```
📄 镜像备份已保存：{约束skill}/.cursor/{app名}/{用户提问主题}_{yyyy}-{M}-{d}_{H}-{m}.md
```

说明：这一行是**附加提示**，前面的对话正文仍须完整可读；路径写**相对 workspace 或绝对路径**均可，须含 `{app名}` 文件夹。

### 5.5 何时不写镜像备份 md

下列情况**只对话、不写文件**（对话仍按场景作答，不受双份约束）：

- 用户仅闲聊、未要求功能解读
- 仅一句话即可回答的问题
- 用户明确说「不要保存文件」「不要备份」

其余凡触发本规则的功能解读，**对话 + 镜像备份 md 双份都必须有**。

### 5.6 图片

默认输出 **Markdown + mermaid**（不单独生成 png）。用户明确要求图片时，可额外说明；技术解读仍以 md 为准。

---

## 6. 输出语言

- 正文与解读：简体中文
- 路径、类名、方法名、常量名：保持原文（**须与中文业务含义成对出现**，见 §1.7）
- 流程图、表格、场景说明：默认 **「代码锚点 + 中文业务含义」双语**，兼顾开发与产品共读

---

## 7. 用户提问模板（规则内自检用）

用户若 @ 本规则并给出**功能名或代码锚点**，即视为触发 **§2.0 链接目录** + **对话完整解读** + **全场景枚举** + **§1.10 阶段清点 + §2.11 分阶段详细说明** + **§1.11/§2.12 广告位专表（涉广告时）** + **§1.11.8 预加载阻塞清点** + **§1.11.9/§2.13 广告请求全链路确认清单（接入/涉广告时，须用户清单确认）** + **§1.11.10 AB 中途切换（有 AB 时）** + **§1.11.11 开屏标准链路** + **§1.11.12/§1.11.13 重试与补货** + **`{约束skill}/.cursor/{app名}/` 镜像备份 md**（双份并存，与它有关阶段不得省略）。
