---
description: AI 协作通用约束(用于接入需求、Bug修复和任务)
alwaysApply: true
---

# AI 协作通用约束

## 0. 规则标识（每次回复必显）

本规则已启用（alwaysApply: true）。**每次回复的第一行**必须输出：

> 📌 已启用约束：`接入需求约束`

要求：

- 放在回复最开头，正文之前
- 纯问答、方案讨论、写代码交付时都要显示
- **Bug 修复 / 问题修复**类任务可在同条或次行补充：`📌 已启用约束：Bug修复门禁（§0.6）`
- 不要省略或合并到其他段落里

## 0.3 门禁总览（实现/接入类需求**强制**）

改代码须按顺序通过下列关卡，**不得跳关**：

| 关卡 | 章节 | 用户须确认 | 本关产出 |
|------|------|-----------|----------|
| **第一关** | §0.4 需求理解 | 「理解确认」 | 文字理解 + **流程图（每节点 `(新增)/(修改)/(删除)`）** + **影响表（必填）** |
| **第二关** | §0.5 约束与路径 | 「确认」 | 约束自检 + workspace/工程路径 |
| **第三关** | §11 改代码前 | 「可以写代码」/「开始执行」 | 路径复述 + `git status`/`git diff` + 实现方案 |
| **执行** | §1～§6 | — | 小步改文件（§3）+ 交付清单 |

**核心原则**：

1. **两轮（实为三轮）门禁**：先「理解确认」→ 再「约束确认」→ **改代码前 §11** → 最后才写代码。
2. **影响表必填**：改**共用类、配置、埋点入口**等时，§0.4.2 第 7 项须列「可能影响哪些已有功能」；无影响须明确写「无影响」。
3. **流程图必带改动标签**：每个节点末尾 `(新增)/(修改)/(删除)`，避免误删、误改。
4. **§3 小步交付**：每轮默认 ≤2 文件；预计 >3 文件或 >100 行须**先报备**并等用户同意。
5. **路径与 Git**：改代码前须复述 workspace 根路径 + 拟改文件绝对路径；须执行 `git status`/`git diff`，有 WIP 须说明合并策略，**未经确认不覆盖**。

## 0.4 需求理解先行（**强制，第一门禁**）

当用户提出**接入某需求、接入某能力、实现某功能、改某逻辑**时，在输出约束自检、实现方案或代码之前，**必须先说明 AI 对该需求的理解**，并**等待用户确认理解正确**后，才进入 §0.5 第二门禁。

### 0.4.1 触发词

与 §0.5.1 相同：`接入` `实现` `开发` `加上` `做一个` `改一下` `帮我写` `落地` `对接` `集成` `迁移` `重构` 等**指向写代码或改工程**的表述。

**不适用**：纯问答、读代码解释、Review、仅要文档/方案且用户明确「先不要写代码」——仍显示 §0 标识，但可跳过 §0.4 / §0.5 确认块。

### 0.4.2 必出内容（模板）

每次实现/接入类需求，**第一轮回复**必须输出以下块：

```markdown
## 🎯 需求理解（请先确认理解是否正确）

### 1. 需求背景
<用户要解决什么问题，1–2 句>

### 2. 核心目标
<做完后应达到什么效果，1–3 句>

### 3. 范围边界
- **包含**：…
- **不包含**：…

### 4. 关键约束 / 假设
- …（标注「待确认」的项须明确写出）

### 5. 验收标准（草案）
- [ ] …
- [ ] …

### 6. 需求理解流程图

> 下图基于需求文档梳理，**每个节点/任务名称末尾必须标注改动类型**：`(新增)` / `(修改)` / `(删除)`；无法判定时标注 `(待确认)` 并说明原因。

\`\`\`mermaid
flowchart TD
    Start([入口/触发条件(新增)]) --> Check{条件判断(修改)}
    Check -->|分支A| TaskA[任务A描述(修改)]
    Check -->|分支B| TaskB[任务B描述(新增)]
    TaskA --> End([结束/交付(修改)])
    TaskB --> End
\`\`\`

**图例说明**：
- `(新增)`：需求文档要求新建的能力、页面、接口、字段等
- `(修改)`：在现有代码/逻辑/配置上调整
- `(删除)`：需求文档要求移除的能力、入口、字段等
- `(待确认)`：文档未明确或需对照代码才能判定

### 7. 已有功能影响评估（必做）

> 实现本需求前，须对照项目代码分析是否影响**非本需求目标**的已有功能；**有则必须单独说明，无则须明确写「无影响」**。

| # | 已有功能（中文名） | 关联代码位置 | 影响类型 | 可能影响的表现 | 风险等级 |
|---|-------------------|-------------|----------|----------------|----------|
| I1 | 例：开屏广告展示 | `SplashAdManager.kt`（开屏广告管理） | 修改共用逻辑 | 开屏展示时机变化 | 高 |
| I2 | … | … | … | … | 高/中/低 |

**影响类型**（择一或多选）：`修改共用组件` / `修改共用接口` / `修改配置或开关` / `删除或下线入口` / `变更时序或生命周期` / `无影响`

**无影响时**须明确写出：

> 经检索，本需求改动范围限于 `<模块/文件>`，**未发现对其它已有功能的影响**。

**请你确认**：
1. 以上需求理解（含流程图）是否与需求文档一致？
2. 以上**已有功能影响评估**是否准确？是否接受这些连带风险？（有影响的条目须确认；若需规避某功能请指出）

回复「理解确认」或指出偏差；**理解确认后**，我再输出约束自检与路径确认。
```

### 0.4.3 流程图硬性要求

1. **必须覆盖需求文档要点**：主流程、关键分支、异常/边界路径（文档有则必须画进图里）。
2. **每个任务节点必须带改动标签**：节点文案格式为 `任务描述(新增|修改|删除|待确认)`，**标签放在节点名称末尾**。
3. **分支与决策**：条件判断用菱形 `{}`；主路径与异常路径均须体现，文档中的 P0 分支不可省略。
4. **与文字理解一致**：流程图须与 §0.4.2 的 1–5 项文字描述一致；若有冲突，以文档为准并标注矛盾点。
5. **简单需求**：即使只有 3–5 个节点，也须出图；禁止用纯文字替代流程图。
6. **无需求文档时**：基于用户描述与现有代码推断，所有 `(待确认)` 节点须在 §0.4.2 第 4 项列出。
7. **已有功能影响必评估**：改**共用类、基类、工具类、全局配置、路由、埋点统一入口、广告闸门**等时，**必须**填写 §0.4.2 第 7 项影响表；禁止写「应该没影响」而不检索。
8. **影响须有代码依据**：须通过类引用、调用链、全局搜索支撑每一项影响；无法确定时标 `(待确认)` 并说明拟如何验证。
9. **高风险须给对策**：风险等级为「高」的条目，须在后续实现方案中给出**隔离或回滚**思路，未经用户确认不得改动对应共用模块。

### 0.4.4 第一门禁停止点

输出 §0.4.2 全文后，**必须停止**，并写明：

> **请确认以上需求理解（含流程图）与已有功能影响评估是否正确。你回复「理解确认」或补充修正后，我再进入约束自检与路径确认。未经理解确认，不出约束自检、不出实现方案、不写代码。**

**禁止**：跳过需求理解直接出约束自检；禁止流程图节点缺少 `(新增)/(修改)/(删除)` 标签；禁止跳过 §0.4.2 第 7 项影响评估。

## 0.5 实现/接入类需求门禁（**强制，第二门禁**）

当用户提出**接入某需求、接入某能力、实现某功能、改某逻辑**时，**须先通过 §0.4 需求理解确认**，再进入本节；并在输出任何实现说明、方案、步骤、文件清单或代码之前，**停下来让用户确认「已按约束执行」**。

### 0.5.1 触发词（命中即适用本节）

`接入` `实现` `开发` `加上` `做一个` `改一下` `帮我写` `落地` `对接` `集成` `迁移` `重构` 等**指向写代码或改工程**的表述。

**不适用**：纯问答、读代码解释、Review、仅要文档/方案且用户明确「先不要写代码」——仍显示 §0 标识，但可跳过 §0.4 / §0.5 确认块。

### 0.5.2 回复顺序（不得打乱，两轮门禁）

**第一轮（需求理解门禁 · §0.4）**

1. **第一行**：`📌 已启用约束：接入需求约束`（§0）
2. **第二块（必出）**：**§0.4 需求理解**（含 Mermaid 流程图、改动标签、**已有功能影响评估**）
3. **停止**：等待用户「理解确认」或修正意见（含对影响面的确认或规避要求）
4. **禁止**在本轮输出约束自检、路径确认、实现方案或代码

**第二轮（约束与路径门禁 · §0.5，须用户已完成理解确认）**

5. **第一行**：`📌 已启用约束：接入需求约束`（§0）
6. **第二块（必出）**：**「约束自检清单」**（模板见 §0.5.3）
7. **第三块**：**项目路径确认**（§8）——写明将修改的**完整工程路径**
8. **停止**：输出「**请确认以上约束与路径；你回复「确认」或补充修改后，我再给出实现说明/方案。未经确认不写代码。**」
9. **用户确认后**，才允许进入 §1～§2：分支表（若仍模糊）、实现步骤、风险、验收清单等
10. **用户明确同意方案并允许写代码后**，须先完成 **§11 改代码前门禁**（路径复述 + git 检查），再允许改文件

**禁止**：跳过 §0.4 理解确认；跳过 §0.5 约束自检直接给实现方案或写代码；跳过 §11 直接改文件；禁止在用户未确认约束自检前声称「已按你的要求实现」。

### 0.5.3 约束自检清单（模板，理解确认**之后**必贴）

用户完成 §0.4「理解确认」后，在出方案**之前**原样输出以下块（勾选 `[x]` 表示本次**将遵守**，`[ ]` 表示本次不涉及）：

```markdown
## 📋 约束自检（请确认后我再出实现说明 / 写代码）

> 需求理解已确认 ✅。以下为本轮将遵守的约束，**未经你确认我不会进入方案细节或写代码**。

| 约束条目 | 本次是否遵守 | 说明（一句话） |
|----------|-------------|----------------|
| §0.4 需求理解先行 | [x] | 已输出理解+流程图，用户已确认 |
| §0.4 已有功能影响 | [x] | 已列出影响面（或无影响说明），用户已确认 |
| §1 先方案后代码 | [x] | 先步骤+风险，不写代码 |
| §2 模糊需求先澄清 | [ ]/[x] | 分支/场景表或标注「需求已清晰」 |
| §3 小步交付 | [x] | 每轮≤2 文件，大范围先报备 |
| §4 边界与异常 | [x] | 空值/弱网/竞态等默认考虑 |
| §5 最小改动 | [x] | 对齐现有写法，不顺手重构 |
| §8 项目路径确认 | [x] | 路径：`<完整路径>` |
| §11 改代码前门禁 | [x] | 写代码前路径复述 + git status/diff |
| §9 代码名词双语 | [x] | 方案中含代码名须附中文含义 |
| §10 埋点接入 | [ ]/[x] | 查重复上报 + 上报时打印事件名与参数 |
| `code-term-bilingual` | [x] | 准确表述，禁止误导性「必定/总是」 |
| 其它已 @ 规则 | [ ]/[x] | 如 `cursor-feature-interpret` 等 |

**请你确认**：回复「确认」或指出要调整的约束/路径；确认后我才输出实现说明（步骤、文件、风险、验收清单）。
```

可根据任务勾选「不涉及」并简述原因，但**不得删除**「先理解确认 → 再约束确认 → §11 改代码前检查 → 后方案 → 再代码」的门禁说明。

### 0.5.4 用户确认什么（两轮）

**第一轮（§0.4）** — 用户确认：

1. AI 对需求的**文字理解**与需求文档一致
2. **流程图**正确反映文档中的流程、分支与任务，且改动标签 `(新增)/(修改)/(删除)` 合理
3. 范围边界、假设、验收标准草案无重大偏差
4. **已有功能影响评估**准确：接受连带风险 / 要求规避某功能 / 确认「无影响」说明成立

**第二轮（§0.5）** — 用户确认：

1. **AI 已声明将遵守**上述约束（而非 AI 自行发挥、跳过先方案后代码）
2. **工程路径正确**（§8）
3. （可选）对约束清单有补充或例外——例外须用户**书面同意**后才可偏离

未收到对应轮次的「理解确认」/「确认」或等价明确同意前：**禁止**进入下一轮门禁，**禁止**输出详细实现方案作为最终定论，**禁止**写代码、改文件、跑破坏性命令。

## 0.6 Bug 修复 / 问题修复门禁（**强制**）

当用户提出**修 bug、修复问题、排查后不展示/闪退/异常、回退某次改动**等**指向修复既有缺陷**的表述时，适用本节；与 §0.4 / §0.5 **并行**，不替代「先说明 → 用户确认 → 再改代码」。

### 0.6.1 触发词（命中即适用本节）

`修 bug` `修复` `fix` `bug` `不展示` `不显示` `闪退` `崩溃` `异常` `失效` `回退` `为什么坏了` `全部没了` `排查` 等**指向修复既有问题**的表述。

**不适用**：纯问答「可能是什么原因」（用户明确只要分析、不要改代码）——只输出 §0.6.2 根因分析块，**禁止改文件**。

**与 §0.4 的关系**：
- **新功能 / 接入需求** → 走 §0.4 + §0.5
- **修复已有问题** → 走 §0.6（若同时含新需求，两条门禁均须满足）

### 0.6.2 改代码前必出：Bug 清单与修复方案（模板）

**第一轮回复**在动任何文件之前，必须逐条输出：

```markdown
## 🐛 Bug 修复方案（请先确认后再改代码）

| # | 现象 / 用户描述 | 根因（为何发生） | 拟修改文件 / 方法 | 修复思路（怎么改） | 可能影响其它已有功能 | 风险 / 副作用 |
|---|----------------|------------------|-------------------|-------------------|---------------------|---------------|
| B1 | … | … | `Xxx.kt` / `method()` | … | 例：开屏广告展示时机变化 | … |
| B2 | … | … | … | … | 无影响 / … | … |

### 修复顺序（建议）
1. B1 → …
2. B2 → …

### 不在本次范围（若适用）
- …

**请你确认**：以上根因与修复思路是否正确？回复「确认修复」或指出要调整/排除的条目；**未经确认禁止改代码**。
```

**硬性要求**：

1. **一条 Bug 一行**：用户说「全部不展示」须拆成可验证的子项（如开屏、Banner、插屏、原生各一条），**禁止**笼统写「修广告」。
2. **根因须可验证**：写清触发条件 + 代码/时序/配置原因；不确定则标 `(待验证)` 并说明打算如何验证，**禁止**无依据猜测后直接改。
3. **修复思路须对应根因**：每条 Bug 的「怎么改」须能解释「为何能消除根因」。
4. **敏感操作**（回退、删文件、改配置、动广告闸门/AB 面）须在表中单独标注，等用户同意。
5. **连带影响必说明**：「可能影响其它已有功能」列须对照 §0.4.2 第 7 项口径填写；改共用代码时不得留空或写「应该没事」；无影响须写「无影响」。

### 0.6.3 改代码后必出：修复对照清单（模板）

用户确认并允许改代码、改动完成后，**必须**输出与 §0.6.2 **编号一一对应**的对照表：

```markdown
## ✅ Bug 修复对照清单

| # | 现象 | 根因（摘要） | 计划修复 | 实际改动 | 状态 |
|---|------|-------------|----------|----------|------|
| B1 | … | … | … | `path/to/File.kt`：… | ✅ 已完成 / ❌ 未完成 / ⏸ 未做（原因） |
| B2 | … | … | … | … | … |

### 改动文件清单
| 文件 | 操作 | 关联 Bug # |
|------|------|-----------|
| … | 修改 | B1 |

### 未覆盖 / 留待后续
- …

### 建议你本地验证（≤5 条）
1. …
```

**硬性要求**：

1. **编号不得遗漏**：§0.6.2 中每一行 Bug 在对照表中必须有对应行；未修的须标 ❌ 或 ⏸ 并说明原因。
2. **「计划修复」与「实际改动」须可追溯**：实际改动与方案不一致时，须解释为何偏离。
3. **禁止**声称「已修好」而不附对照清单；**禁止**只贴 diff 不对照条目。

### 0.6.4 用户确认什么

用户须确认：

1. **Bug 拆分与根因**是否认同（或指出哪条不对）
2. **修复思路与范围**是否同意（含不做的项）
3. **可能影响其它已有功能**是否准确，是否接受连带风险或要求规避
4. 明确回复 **「确认修复」** 或 **「开始执行」** 后，仍须完成 **§8.1 + §11**，再允许改文件

用户仅问「问题在哪 / 什么原因」且未要求修复时：**只出 §0.6.2 的前两列（现象+根因）**，停止于分析，**禁止改代码**。

### 0.6.5 与其它章节的关系

- 与 §1：Bug 修复同样 **先方案后代码**；§0.6 为 Bug 场景的**专用模板**
- 与 §3：Bug 修复也遵守小步交付；多条 Bug 可先修 P0，对照清单标明其余 ⏸
- 与 §6：Bug 修复完成时，**§0.6.3 对照清单替代或补充** §6 的验收清单
- 与 §7：**禁止**跳过 §0.6.2 直接改代码；**禁止**用户只问原因时擅自修复；**禁止**跳过 §11

## 1. 先方案，后代码

- **Bug 修复 / 问题修复**类任务须先完成 **§0.6 Bug 清单与修复方案**，获用户「确认修复」后再改代码；完成后须输出 **§0.6.3 修复对照清单**。
- 收到实现类需求时，**须先完成 §0.4 需求理解（含流程图、已有功能影响评估）并获用户「理解确认」**，再完成 **§0.5 约束自检并获用户确认**，然后输出实现步骤、会改哪些文件、风险点；**不写代码**。
- 用户明确允许写代码后，**须先完成 §8.1 路径复述 + §11 Git 检查**，获用户「可以写代码」/「开始执行」后，才允许改文件。
- 实现方案须与 §0.4.2 第 7 项影响表一致；**未获用户对影响面确认前，禁止改动高风险共用模块**。
- 多文件改动、重构、删改现有逻辑、或可能影响线上/生产行为时，**必须等用户确认方案后再动手**。
- **敏感/有风险操作**须先说明并等用户同意：删文件、改架构、改依赖/配置、改权限与安全相关项、动核心业务链路。
- 需求不清晰时先提问，不假设、不自行扩展 scope。
- 用户未明确说「可以写代码」前，**禁止写代码**。

## 2. 模糊需求澄清（理解确认后、实现前）

需求文档不完整、分支未列全、或存在矛盾时，在 §0.4 理解确认**之后**、写代码**之前**，**禁止直接实现**，补充输出：

1. **核心目标复述**（可与 §0.4 对齐，有变化须说明）
2. **分支/场景表**：

   | 场景 | 触发条件 | 预期行为 | 改动类型 | 优先级 | 状态 |
   |------|----------|----------|----------|--------|------|
   | … | … | … | 新增/修改/删除 | P0/P1/P2 | 待确认 |

3. **文档缺失/矛盾项** + 你的假设（标注「待确认」）
4. **两种实现思路**（简单 vs 完整）：改动范围、风险、推荐项

用户确认分支表与方案后，才进入实现。

## 3. 需求拆解与小步交付

- 一次只做一个明确目标；需求过多时主动建议拆分。
- **分步实现**：一次只做一个分支或一个可验收小任务。
- 每轮默认最多改 **2 个文件**；预计超过 **3 个文件或 100 行** 时，**必须先说明范围并等用户同意**（大范围先报备，禁止一轮堆完）。
- **影响面扩大时须停**：若实现中发现需改共用类/配置/埋点入口或影响表外功能，须暂停、更新 §0.4.2 第 7 项影响表并重新确认。
- 实现前列出**验收清单**；用户未给清单时，先补简短清单供确认。
- 未覆盖的分支可留 TODO，**不要一轮全部实现**。
- 完成后逐项对照清单，不默认「主流程做完即完成」。

## 4. 边界与异常（默认必考虑）

除非用户明确说不需要，否则必须考虑：

- **数据异常**：空值、空列表、字段缺失、格式不符、分页末页/搜索无结果
- **网络异常**：请求失败、超时、弱网；优先复用项目已有重试/超时/取消机制
- **权限与状态**：未登录、无权限、重复提交、并发/竞态
- **UI 状态**：loading、error、empty；提交/请求中 disabled，防重复点击
- **生命周期**（如适用）：页面销毁后异步回调、组件卸载后仍更新状态

处理方式：先搜索项目中同类功能的写法，**复用现有错误处理、请求封装、提示组件**，不另起一套。

## 5. 代码简洁与最小改动

- **最小可用实现优先**；不过度抽象，不做用户未要求的「扩展性」预留。
- 存在多种写法时，默认选 **改动更小** 的方案；另一种仅作对比说明。
- 用户指定参考文件，或项目内有同类实现时，**必须对齐其写法**（命名、分层、错误处理一致）。
- 不为 1–2 处调用新建 helper/hook/抽象层；不顺手重构周边代码。
- 写代码前阅读周边文件，匹配现有命名、目录、类型与风格。
- 用户可见文案走项目既有国际化/常量规范，不硬编码（若项目有约定）。
- 注释用简体中文，只解释非显而易见的业务逻辑。

## 6. 交付格式

完成时简要说明：

1. **验收清单对照**（✅ 已完成 / ❌ 未完成）；**Bug 修复类任务须附 §0.6.3 修复对照清单，与改前 Bug 编号一一对应**
2. **改动文件清单**（每个文件一句话说明改了什么，并标注 新增/修改/删除）
3. **关键逻辑位置**：入口、分支判断处、错误提示处（类名/方法名即可，不必贴全文）
4. **已覆盖 / 未覆盖**的边界情况（及原因）
5. **验证步骤**（≤5 条，可手工执行）

补充要求：

- 复杂流程（超过 3 个分支）用 **Mermaid 简图** 说明主路径与异常分支；节点仍须带 `(新增)/(修改)/(删除)` 标签。
- **禁止在回复中贴大段无关代码**；必要时只贴关键 10–30 行并说明作用。
- 用户指出遗漏时，**只补必要代码**，不重写整块逻辑。

## 7. 禁止事项

- **跳过 §0.6**：Bug/问题修复未输出 Bug 清单（根因+修复思路）、未获「确认修复」就改代码；用户只问原因时擅自修复
- **跳过 §0.4**：未输出需求理解、流程图（含改动标签）、或未获「理解确认」就进入约束自检或方案
- **跳过已有功能影响评估**：未单独说明对其它已有功能的影响（或无影响说明）、或未获用户确认就改共用代码
- **跳过 §0.5**：未贴约束自检、未获用户确认就出方案或写代码
- **跳过 §11**：未复述路径、未执行 git status/diff、或有 WIP 未说明合并策略就改文件
- **路径不一致仍改代码**：workspace 根路径或拟改文件路径与用户指定不一致时未停止询问
- **流程图缺标签**：任务节点未标注 `(新增)/(修改)/(删除)`
- 不假设 happy path 即完成
- 不一次性堆大量功能
- 不擅自扩大 scope（「顺便做了 XX」需先征得同意）
- 不引入与任务无关的新依赖、新架构层、大规模重构
- 不提交含密钥/凭证的文件
- 不执行破坏性 git 操作（除非用户明确要求）

## 8. 项目路径确认（实现/接入类需求**必做**，与 §0.5 同步）

- 在 §0.5 约束自检表中**必须写出**将修改的工程**完整绝对路径**。
- 存在同名项目（如多个 `videodownload`）时，**禁止假设**当前 workspace 即用户意图；须请用户确认路径后再继续。
- 路径未确认前，**禁止**改文件。

### 8.1 改代码前路径复述（**强制**，与 §11 同步）

**每一次**即将改文件之前（含 Bug 修复、小步交付的每一轮），必须在回复中**显式复述**：

```markdown
## 📂 路径确认（改代码前）

| 项 | 路径 |
|----|------|
| **当前 workspace 根路径** | `<实际 workspace 根绝对路径>` |
| **用户指定工程路径** | `<用户任务中指定的路径，若无则写「未指定，以 workspace 为准」>` |
| **本轮拟修改文件（绝对路径）** | 1. `<path/to/File1.kt>` … |

**一致性**：`[ ] 一致，可继续` / `[ ] 不一致，已停止并询问`
```

**硬性要求**：

1. **路径与用户指定不一致时**：**立即停止**，说明差异（如 workspace 为上级目录、拟改文件属于另一子工程），**询问用户**正确路径；**禁止**在未确认前改任何文件。
2. **不得只用相对路径糊弄**：拟改文件须给出**可唯一定位的绝对路径**（或 workspace 根 + 相对路径，二者须成对出现）。
3. **每轮改代码前都复述**：即使上一轮已确认过，新一轮改不同文件时仍须再复述。

## 11. 改代码前门禁（**强制，第三关**）

用户已「理解确认」+「约束确认」并**明确允许写代码**后，动任何文件之前须完成本节；与 §8.1 路径复述**同条回复**输出。

### 11.1 触发

凡将执行：`write` / `search_replace` / 删除文件 / 跑会改工程的命令，均适用。

### 11.2 必做：Git 工作区检查

**须实际执行**（或等价只读查看）`git status` 与 `git diff`（含 `git diff --staged`），并在回复中输出：

```markdown
## 🔍 Git 工作区检查（改代码前）

### git status 摘要
- 当前分支：`<branch>`
- 未提交改动文件：`<列表，无则写「无」>`

### 与本轮拟改文件相关的 diff 摘要
| 文件 | 是否有未提交改动 | 改动性质（一句话） |
|------|-----------------|-------------------|
| `<绝对路径/File.kt>` | 是/否 | … |

### WIP 合并策略（有相关改动时必填）
- **文件** `<path>`：现有未提交改动为 …；本轮计划在**其基础上追加** / **需你确认是否覆盖** / **建议你先 stash**
- **未经你确认，禁止覆盖**你已改动的行或整文件回写。

**请你确认**：路径无误 + Git/WIP 策略可接受后，回复「可以写代码」或「开始执行」；我才会改文件。
```

### 11.3 硬性要求

1. **目标文件有未提交改动时**：必须在表中列出，说明**将如何与现有改动合并**；**未经用户确认，禁止覆盖**。
2. **发现 diff 与方案不一致时**：须暂停，说明差异并更新方案或请用户决定。
3. **禁止**跳过 git 检查直接改代码；**禁止**声称「已修改」而未在本轮或上一轮完成 §11 检查（小步交付的后续轮次仍须检查）。
4. **Bug 修复**同样适用：§0.6「确认修复」之后、改文件之前仍须 §8.1 + §11。

### 11.4 与其它章节的关系

- 与 §0.3：第三关；**理解确认 → 约束确认 → §11 → 写代码**
- 与 §8.1：路径复述与 Git 检查**同一次回复**输出
- 与 §3：每小步改代码前仍须 §11（可仅检查本轮涉及的文件）
- 与 §7：跳过 §11、路径不一致仍改、覆盖 WIP 均属禁止项

## 9. 代码名词双语（与 `code-term-bilingual` 联动）

回复中凡出现类名、方法名、配置 key、标志位等代码名词，须附**中文业务含义**，产品可读；**禁止**无条件的「必定/总是/永远」简写（如「开屏必定 A 配置」——须写清 commit 前后分支）。详见 `@code-term-bilingual`。

## 10. 埋点接入（Telemetry / 事件上报）

当用户提出**接入/新增/修改埋点、事件上报、Telemetry** 时，本节与 §0.4 / §0.5 **并行**，不替代「先理解确认 → 再约束确认 → 后方案 → 再代码」。

### 10.0 规则标识（埋点类任务必显）

埋点类任务回复中须明确写出（可与 §0 标识同条或紧随其后）：

> 📌 已基于约束 §10 完成重复上报检查

- 纯问答、读代码解释、与埋点无关的任务：**不强制**显示本标识
- 埋点类任务：**禁止**在未完成 §10.2 检查前写代码或声称「已接入完成」

### 10.1 触发范围

命中以下任一表述即适用本节：

`埋点` `上报` `事件` `Telemetry` `trackShow` `trackClick` `KiteAnalytics` `TelemetryCatalog` `AppsFlyer` `Adjust` `Firebase Analytics` `logEvent` `emit(`

### 10.2 重复上报检查（写代码前必做）

在给出实现方案或改文件**之前**，对**每一个**拟新增/修改的事件名，完成下列检查并写入回复。

#### 10.2.1 调用链追溯

从业务触发点（如 `onAdImpression`、按钮点击、生命周期）向下追踪，列出该事件名的**全部上报出口**：

| 检查项 | 本工程常见入口 |
|--------|----------------|
| Firebase | `KiteAnalytics.emit` / `TelemetryCatalog.*.emit` |
| AppsFlyer | `AppsFlyerTracker.*`（常由 `KiteAnalytics` 内 `when` 分支触发） |
| Adjust | `AdjustSdkUtil.trackEvent`（常由 `KiteAnalytics` 或 `DeferredAttributionBridge.emit` 触发） |
| 广告专用 | `AdTracker.trackShow` / `trackClick` → 内部再 `emit` |
| 桥接空参 | `DeferredAttributionBridge.emit(event)` → **空 Bundle** 再打 Firebase |

对每条链路注明：**文件 + 方法 + 参数是否带业务字段**（如 `ad_unit_id`）。

#### 10.2.2 重复判定

满足任一即视为**重复上报**，须 §10.3 处理：

1. **同名事件**在单次业务动作中被调用 **≥2 次**（含不同封装：如 `AdTracker.trackShow` + `DeferredAttributionBridge.emit(AD_SHOW)`）
2. **同一出口**被两条路径同时打到（如 `KiteAnalytics.emit` 被 `AdTracker` 与 `DeferredAttributionBridge.emit` 各调一次）
3. **参数不一致的双发**：一条带参、一条空 Bundle，GA4 表现为同名事件条数翻倍且约一半缺参

#### 10.2.3 检索命令（须实际执行或等价搜索）

- 全局搜事件名字符串：`TelemetryCatalog.<name>`、`"<event_name>"`、`trackEvent("<event_name>")`
- 全局搜拟改动文件中对 `emit(`、`trackShow`、`trackClick`、`DeferredAttributionBridge` 的引用

### 10.3 发现重复时（强制，先于改代码）

**禁止**在未与用户沟通前直接删改代码。须先输出：

```markdown
## ⚠️ 重复上报风险提示（§10）

**事件名**：`<event_name>`
**重复路径**：
1. `<路径 A>` — 参数：`<有/无 ad_unit_id 等>`
2. `<路径 B>` — 参数：`<…>`

**数据影响**：如 GA4 条数约 ×2、空参占比约 50% 等（按实际推断）

**建议修复方向**（仅方案，不写代码）：保留单一路径 `<推荐入口>`，移除/合并 `<另一入口>`；Adjust/AF 若需保留则并入同一路径的 Bundle。

请确认是否按上述方向修复；你回复「确认」后我再改代码。
```

### 10.4 无重复时的输出（接入方案必含）

```markdown
## ✅ 重复上报检查（§10）

| 事件名 | 上报出口（单一路径） | 参数来源 | 结论 |
|--------|---------------------|----------|------|
| `<name>` | `<例如 AdTracker → KiteAnalytics → Firebase+AF+Adjust>` | `<Bundle 字段>` | 无重复 |

**检查方式**：已全局检索 `<event_name>` 及 `emit`/`track*` 引用。
```

随后才可进入 §0.5 约束自检与实现说明。

### 10.5 本工程反例（勿复现）

`ad_show` 曾在曝光回调中同时调用：

- `AdTracker.trackShow`（带 `ad_unit_id` / `ad_platform` / `ad_sense`）
- `DeferredAttributionBridge.emit(AD_SHOW)`（空 Bundle 再打 Firebase）

导致 GA4 约一半 `ad_show` 无参。修复原则：**广告展示/点击只走 `AdTracker`**，勿再经 `DeferredAttributionBridge.emit` 发同名事件。

### 10.6 上报时 Logcat 日志（强制）

**每一次**事件实际上报（Firebase / AF / Adjust 等经统一入口发出）时，**必须**在 Logcat 打印**事件名 + 完整参数**，便于联调与排查重复/缺参。

#### 10.6.1 优先复用现有实现（禁止旁路裸打）

本工程已有统一日志能力，**必须复用**，不得在业务层重复造轮子或直接 `firebaseAnalytics.logEvent` 而不打日志：

| 组件 | 路径 | Logcat Tag | 说明 |
|------|------|------------|------|
| **Event 模块统一入口** | `Event/.../KiteAnalytics.kt` → `emit()` | `TAG-->>isi_埋点` | 上报前已 `Log.i`，格式：`事件=<name>  参数=<键值对>` |
| **全量埋点日志** | `Event/.../EventAllLog.kt` → `log()` | `TAG-->>eventAll` | 由 `KiteAnalytics.emit` 内部调用；格式：`事件=<name> \| 参数=<键值对>` |
| **参数格式化** | `KiteAnalytics.formatBundleForLog(bundle)` | — | 将 `Bundle` 格式化为可读键值串，**新增入口须复用** |
| **App 层封装** | `app/.../telemetry/KiteAnalytics.kt` → `emit()` | `NitroPdf埋点` | 已有 `事件=<name> 参数=<params>`；**App 层新增上报须走此对象或 Event 模块 `emit` 扩展** |
| **封装 Reporter** | `TelemetryReporter.*` | （经 `emit` 间接输出） | 业务优先调用 Reporter，底层仍须落到带日志的 `emit` |

**接入原则**：

1. **新增埋点** → 使用 `TelemetryCatalog.xxx.emit(bundle)` 或 `KiteAnalytics.emit(event, bundle)`，**禁止**绕过统一入口。
2. **已有 Reporter 方法** → 优先扩展 `TelemetryReporter`，内部仍调用 `emit`。
3. **广告类事件** → 走 `AdTracker.trackShow` / `trackClick` / `trackRequest*`，勿在业务层再 `emit` 同名事件。
4. **若必须新增非 Firebase 直上报口**（如 Meta `FacebookEventsDelegate`）→ 在实际上报前/后调用 `EventAllLog.log(eventName, bundle)`，或同等格式自建日志（**事件名 + 全部参数**）。
5. **禁止**仅打印事件名不打印参数；**禁止**在 Debug 才打印（埋点日志须 **Release 也可查**，与 `AdRequestLog` 策略一致）。

#### 10.6.2 Logcat 过滤命令

```bash
adb logcat -s "TAG-->>eventAll:I" "TAG-->>isi_埋点:I" "NitroPdf埋点:I"
```

#### 10.6.3 验收（埋点任务必含）

接入完成后，在验收清单中增加：

- [ ] 触发一次业务动作，Logcat 可见对应**事件名**
- [ ] 同一条日志含**全部业务参数**（与 `Bundle` 一致，含 `attr_*` 等）
- [ ] 无重复路径导致同动作出现两条同名事件日志（或已按 §10.3 修复）

### 10.7 与其它章节的关系

- 与 §0.5：**并行**——埋点接入仍须先约束自检、路径确认、用户「确认」后再写代码
- 与 §9 `code-term-bilingual`：检查说明中的类名/方法名须附中文含义
- 本节**不替代**需求理解与验收清单，强制「先查重 → 上报走统一入口并打日志 → 再方案 → 再代码」
