## 开屏 load/preload 调用点清点（请确认）

> AI：接入/修改开屏前必填；用户回复「**确认调用点**」前禁止改代码。
> 检索命令见 [SKILL.md §开屏调用点清点门禁](../SKILL.md#开屏调用点清点门禁接入前强制--须用户确认)

| # | 文件:行 | 函数 / 触发时机 | 调用 API | 类型 | 金样是否保留 | 说明 |
|---|---------|-----------------|----------|------|--------------|------|
| 1 | `` | UMP 后 SDK 一次性回调 | `preloadAd(LOADING_SPLASH)` | preload | ✅ / ❌ / 待确认 | |
| 2 | `` | Loading 放行后 | `obtainForShow` → `show` | 展示 | ✅ | 只读缓存，非 load |
| 3 | `` | 展示后补货 | `preloadAd` / Replenish | preload | 待确认 | scene=展示消耗后 |

**统计**

- preload **网络**入口：__ 处（金样目标：**1** 处，`scheduleSplashPreloadOnceWhenSdkReady`）
- `loadAd(开屏)` 入口：__ 处（金样目标：**0** 处）
- 展示入口：__ 处

**冲突 / 风险（若有）**

- …

**请你确认**：保留/删除/合并上表哪些 #？回复「**确认调用点**」或指出修改；确认前我不改工程。
